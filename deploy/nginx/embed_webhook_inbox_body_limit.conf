# Phase 1e-F7: edge limit for POST /api/v1/public/embed/v1/hooks/*/inbox
# Align with backend settings.embed_webhook_max_body_bytes (default 1 MiB).
# Include in server/location block that proxies to the API upstream.
client_max_body_size 1m;
