## ARCH_DEV_PPR_MODEL_018_TASKS — сверх‑детализированные dev‑таски

> Связанная архитектура: `ARCH_DEV_PPR_MODEL_018.md`  
> Связанный DEV_PROMPT: `DEV_PROMPT_PPR_MODEL_018` (P2, PPR‑2)

---

### 1. Understand — текущее состояние моделей и связей Paperless

1.1. **Инвентаризация текущих сущностей Paperless.**  
1.1.1. Найти все модели/таблицы, связанные с формами: шаблоны, сабмишены, подписи, легаси‑структуры.  
1.1.2. Зафиксировать, какие поля/статусы используются сейчас и как хранятся связи с пациентом/визитом/клиникой.

1.2. **Разобрать текущие сервисы и API Paperless.**  
1.2.1. В сервисах и роутерах Paperless (PWA и админка) выписать основные операции: создание форм, выдача, подписание, просмотр списков по пациенту/визиту.  
1.2.2. Отметить, где логика статусов «живёт» на фронте или в легаси‑коде.

1.3. **Сопоставить с GAPS и ARCH.**  
1.3.1. Открыть `BACKEND_GAPS_Paperless_NEXT.md`, `ARCH_PAPERLESS_NEXT.md` и QA‑отчёты по формулам.  
1.3.2. Для каждого GAPS PPR‑2 и QA‑заметки указать, какие текущие модели/сервисы к нему относятся (разрозненность статусов, отсутствие связей, слабая прослеживаемость).

---

### 2. Design‑to‑code — целевые сущности и статусы

2.1. **Спроектировать `FormTemplate` и `FormInstance`.**  
2.1.1. На основе раздела 3.1 `ARCH_DEV_PPR_MODEL_018.md` уточнить поля `FormTemplate` и `FormInstance` (или `FormSubmission`), включая `clinic_id`, `patient_id`, `booking_id`, статусы, audit‑поля.  
2.1.2. Убедиться, что модель не дублирует другие домены (Booking/ERP/Loyalty), а только ссылается на них.

2.2. **Зафиксировать финальный enum статусов формы.**  
2.2.1. Принять целевой перечень статусов (`draft`, `issued`, `in_progress`, `signed`, `cancelled`, `expired`, `revoked`) из раздела 2.2.  
2.2.2. Описать допустимые переходы между статусами и требования по audit‑trail’у (кто/когда перевёл).

2.3. **Спроектировать audit‑слой (подписи/действия).**  
2.3.1. На уровне дизайна определить, нужен ли отдельный `FormSignature`/`FormAuditEntry` (раздел 3.1) или достаточно полей в `FormInstance` для текущего vNext.  
2.3.2. Описать минимальный набор событий (`issued`, `filled`, `signed`, `revoked`, `cancelled`) и их поля.

2.4. **Связать Paperless с Booking/Tasks на уровне модели.**  
2.4.1. Для связей с визитом (раздел 4.1–4.3) описать, какие обязательные формы ожидаются по типу визита/услуги и как это отражается в моделях (списки шаблонов, флаги обязательности).  
2.4.2. Зафиксировать, какие события форм будут порождать Tasks/Attention (отсутствующие/просроченные/отозванные формы).

---

### 3. Implement — модели, миграции, сервисы Paperless

3.1. **Ввести/обновить модели `FormTemplate` и `FormInstance`.**  
3.1.1. Привести существующие ORM‑модели к целевой структуре (поля, типы, связи), не ломая ссылочную целостность.  
3.1.2. Добавить enum статусов `FormInstance.status` и поля для `patient_id`, `booking_id`, `clinic_id`, `expires_at`, `signed_at` и audit‑атрибутов.

3.2. **Спланировать и выполнить миграции данных.**  
3.2.1. Разработать миграции БД, которые переводят текущие данные форм в новую схему (включая маппинг старых статусов и связей).  
3.2.2. Предусмотреть безопасные дефолты для старых записей (например, отсутствие связей/статусов помечать как `draft`/`unknown` и отдельно проанализировать).

3.3. **Обновить Paperless‑сервисы под новую модель.**  
3.3.1. Обновить сервисы создания инстансов по шаблонам, так чтобы они всегда заполняли `clinic_id`/`patient_id`/`booking_id` и начальный статус (`draft`/`issued`).  
3.3.2. Ввести методы изменения статусов с централизованной валидацией переходов и логированием (аналог `BookingStatusService`).

3.4. **Обновить API для PWA и админки.**  
3.4.1. Привести DTO и ответы эндпоинтов к новому словарю статусов и структурам `FormTemplate`/`FormInstance`.  
3.4.2. Обеспечить эндпоинты для получения списка форм по пациенту/визиту/статусу, пригодные для «панелей документов» в UI.

---

### 4. Integrate — связи с Booking, Tasks и UI

4.1. **Интегрировать проверки форм в жизненный цикл визита.**  
4.1.1. В местах смены статуса `Booking` (см. `ARCH_BOOKING_NEXT.md`, фасад завершения визита) добавить проверки наличия обязательных `FormInstance` в статусе `signed` для выбранных сценариев.  
4.1.2. Определить стратегию при отсутствии форм: блокировка перехода или создание Attention/Task с указанием недостающих документов.

4.2. **Интеграция с Tasks/Attention.**  
4.2.1. На основе `ARCH_DEV_TASKS_MODEL_020.md` реализовать создание Tasks/Attention при:  
       - отсутствии обязательных форм к моменту визита;  
       - просрочке форм (`expired`);  
       - отзыве подписи (`revoked`), влияющем на уже проведённые визиты.  
4.2.2. Обеспечить ссылки на `form_id`, `patient_id`, `booking_id`, `clinic_id` в задачах и attention‑сигналах.

4.3. **Обновить UI для отображения статуса документов.**  
4.3.1. На фронте (PWA и админка) привести экраны форм к использованию единого словаря статусов (аналог BKG‑статусов, но для Paperless).  
4.3.2. Реализовать удобные представления: список форм по визиту/пациенту с фильтрами по статусу и индикаторами «всё готово/есть проблемы».

---

### 5. Observe — логи, метрики и SEC/RBAC

5.1. **Логирование операций с формами.**  
5.1.1. Добавить структурированные логи при создании, выдаче, подписании, отзыве и отмене форм: `form_id`, `template_code`, `patient_id`, `booking_id`, `clinic_id`, инициатор, действие.  
5.1.2. Убедиться, что в логах нет «лишних» ПД (ФИО, текст формы), только идентификаторы и агрегаты.

5.2. **Метрики по статусам и качеству покрытия.**  
5.2.1. Ввести метрики:  
       - количество форм по статусам (особенно `issued`/`expired`/`revoked`);  
       - долю визитов без полного набора обязательных подписанных форм;  
       - среднее время от `issued` до `signed`.  
5.2.2. Использовать эти метрики в рамках OBS/перф‑мониторинга цепочки «запись → документы → визит».

5.3. **RBAC и доступ к документам.**  
5.3.1. Проверить, что все эндпоинты Paperless используют корректные пермишены и фильтрацию по `clinic_id` в соответствии с `ARCH_DEV_SEC_RBAC_022.md`.  
5.3.2. При необходимости дополнить RBAC‑матрицу ролями/правами для юридических/правовых ролей (просмотр/управление чувствительными формами).

---

### 6. Stabilize — тесты, регрессия и обновление артефактов

6.1. **Покрыть модели и сервисы Paperless тестами.**  
6.1.1. Добавить тесты на создание/выдачу/подписание/отзыв форм, включая сценарии с обязательными связями (`patient_id`, `booking_id`, `clinic_id`).  
6.1.2. Проверить корректность работы статусной машины форм (допустимые и запрещённые переходы).

6.2. **Проверить сценарии интеграции с Booking и Tasks.**  
6.2.1. Добавить тесты на взаимодействие с визитами: поведение при отсутствии обязательных форм, создании Attention/Tasks, корректной работе в «зелёных» сценариях.  
6.2.2. Проверить, что добавление Paperless‑проверок не ломает критичные потоки Booking (миграционный период должен быть безопасен).

6.3. **Синхронизировать документацию и GAPS.**  
6.3.1. Обновить `DEV_PROMPTS_NEXT.md`, указав статус `DEV_PROMPT_PPR_MODEL_018` и краткое описание новой модели форм.  
6.3.2. В `BACKEND_GAPS_Paperless_NEXT.md` и QA‑документах отметить закрытые/уточнённые пункты по PPR‑2.  
6.3.3. При необходимости скорректировать `ARCH_PAPERLESS_NEXT.md` и `ARCH_AUDIT_NEXT.md`, чтобы отразить финальный набор сущностей/статусов и ролей Paperless в бизнес‑цепочках.

## ARCH_DEV_PPR_MODEL_018_TASKS — сверх‑детализированные dev‑таски

> Связанная архитектура: `ARCH_DEV_PPR_MODEL_018.md`  
> Связанный DEV_PROMPT: `DEV_PROMPT_PPR_MODEL_018` (P2, PPR‑2)

---

### 1. Understand — текущее состояние Paperless‑моделей и связей

1.1. **Инвентаризация существующих моделей Paperless.**  
1.1.1. В доменном/ORM‑слое Paperless найти сущности/таблицы:  
       - шаблонов форм (templates);  
       - инстансов/сабмишенов форм;  
       - подписей/аудит‑логов (если есть).  
1.1.2. Для каждой выписать:  
       - поля, относящиеся к пациенту, визиту/booking, клинике;  
       - статусы/флаги;  
       - как хранятся контент/заполненные данные.

1.2. **Инвентаризация связей Paperless ↔ Patient/Booking.**  
1.2.1. В моделях/сервисах Paperless и Booking/Patient найти:  
       - поля `patient_id`, `booking_id`/`visit_id` в формах;  
       - использование этих связей в коде (поиск форм по пациенту/визиту, проверки перед изменением статусов Booking).  
1.2.2. Зафиксировать, где связи отсутствуют/необязательны/используются нестабильно.

1.3. **Инвентаризация статусов форм и логики их смены.**  
1.3.1. По коду сервисов/роутеров Paperless и UI (PWA/Admin) собрать текущие статусы форм (строки/enum) и сценарии:  
       - создание формы;  
       - выдача/отправка пациенту;  
       - заполнение/подписание;  
       - отмена/ревокация.  
1.3.2. Сопоставить с целевыми статусами из `ARCH_DEV_PPR_MODEL_018.md` (`draft`, `issued`, `in_progress`, `signed`, `cancelled`, `expired`, `revoked`).

1.4. **Сопоставление с GAPS и ARCH‑артефактами.**  
1.4.1. Открыть `BACKEND_GAPS_Paperless_NEXT.md` (PPR‑2), `ARCH_PAPERLESS_NEXT.md`, QA‑отчёты:  
       - зафиксировать, какие проблемы (отсутствие статусов, неясные связи с визитами, трудности для админов/врачей) нужно устранить.  
1.4.2. Связать это с `ARCH_BOOKING_NEXT.md`, `ARCH_TASKS_NEXT.md`, `ARCH_DEV_TASKS_MODEL_020.md` — понять, как Paperless должен встраиваться в цепочку «запись → документы → визит».

---

### 2. Design‑to‑code — модели `FormTemplate` и `FormInstance`

2.1. **Спроектировать `FormTemplate` как источник правды по формам.**  
2.1.1. На основе `ARCH_DEV_PPR_MODEL_018.md` оформить доменную/ORM‑модель `FormTemplate` с полями:  
       - `id`, `clinic_id` (если шаблоны зависят от клиники);  
       - `code` (уникальный код формы);  
       - `version`;  
       - `title`, `description`;  
       - `content_schema` (описание полей/структуры);  
       - `is_active`, `created_at`, `updated_at`.  
2.1.2. Определить стратегию версионирования шаблонов (как хранить/отображать старые версии).

2.2. **Спроектировать `FormInstance`/`FormSubmission`.**  
2.2.1. Оформить модель `FormInstance` с полями:  
       - идентификация/связи: `id`, `clinic_id`, `patient_id`, `booking_id` (или `visit_id`), `template_id`;  
       - статус: `status` (enum из целевого набора);  
       - данные: `filled_data` (JSON) с минимально необходимой структурой и без лишних ПД;  
       - временные поля: `signed_at`, `expires_at`;  
       - служебные: `created_at`, `updated_at`, `created_by`, `updated_by`.  
2.2.2. При необходимости спроектировать вспомогательные сущности (`FormSignature`, `FormAuditEntry`) для точного audit‑trail.

2.3. **Спроектировать статусы и переходы форм.**  
2.3.1. Зафиксировать enum статусов `FormStatus` и допустимые переходы (матрица):  
       - `draft` → `issued` → `in_progress` → `signed`;  
       - `issued`/`in_progress` → `cancelled`/`expired`;  
       - `signed` → `revoked` (при особых условиях).  
2.3.2. Согласовать эти переходы с юридическими требованиями (где это критично) и с UX‑потоками (как врач/админ/пациент видят эти изменения).

2.4. **Продумать миграцию старых форм и схем.**  
2.4.1. Оценить, как существующие структуры/данные Paperless будут переведены на `FormTemplate`/`FormInstance`:  
       - как маппятся старые статусы/флаги;  
       - как сохранить истории подписей/состояний без потери юридических свойств.  
2.4.2. Зафиксировать допустимые упрощения/ограничения (например, объединение нескольких legacy‑структур в один тип формы).

---

### 3. Implement — сервисы Paperless и API

3.1. **Реализовать сервис работы с `FormTemplate`.**  
3.1.1. Создать/обновить сервис `PaperlessTemplateService` (или эквивалент) с методами:  
       - создание/обновление шаблонов;  
       - активация/деактивация;  
       - получение шаблонов по клинике/типу/версии.  
3.1.2. Обеспечить, чтобы UI/админка использовали только этот сервис для управления шаблонами (никакой «ручной» работы с таблицами).

3.2. **Реализовать сервис работы с `FormInstance`.**  
3.2.1. Создать/обновить сервис `PaperlessFormService` с методами:  
       - `create_instance(template_id, patient_id, booking_id, ...)`;  
       - изменение статусов с валидацией переходов (матрица из 2.3);  
       - сохранение/обновление `filled_data`;  
       - выборка форм по пациенту/визиту/статусу.  
3.2.2. Встроить проверки SEC/RBAC (`ARCH_DEV_SEC_RBAC_022.md`) в операции, особенно по чтению/изменению форм.

3.3. **Обновить API (PWA и админка) под новые модели.**  
3.3.1. В API роутерах Paperless (PWA/Admin) привести контракты к использованию `FormTemplate`/`FormInstance` и нового словаря статусов:  
       - эндпоинты для списка форм по визиту/пациенту;  
       - эндпоинты для выдачи/подписания/отзыва форм.  
3.3.2. Обеспечить, чтобы UI всегда использовал enum статусов, а не «сырые» строки/флаги.

3.4. **Реализовать audit‑trail (минимальный).**  
3.4.1. Если архитектурно требуется детальный audit‑trail (подписи/изменения), реализовать `FormAuditEntry` или аналог:  
       - запись действий (`issued`, `filled`, `signed`, `revoked`, `cancelled`) с актором и временем.  
3.4.2. Убедиться, что этот слой не нарушает требования по ПД (минимум необходимой информации, строгий доступ).

---

### 4. Integrate — связь с Booking, Tasks и цепочками

4.1. **Интеграция обязательных форм с жизненным циклом визита.**  
4.1.1. На основе `ARCH_BOOKING_NEXT.md` определить для основных типов визитов набор обязательных `FormTemplate`:  
       - хирургия, диагностика, общие консультации и т.п.  
4.1.2. В Booking/сервисах или отдельном слое:  
       - при создании/планировании визита:  
         - создавать необходимые `FormInstance` `draft`/`issued`;  
       - при попытке перевода `Booking` в критичные статусы (`in_progress` → `completed`):  
         - проверять, что обязательные формы находятся в статусе `signed` (или зафиксировать иное поведение по бизнес‑решению).

4.2. **Интеграция с Tasks/Attention.**  
4.2.1. На основе `ARCH_TASKS_NEXT` и `ARCH_DEV_TASKS_MODEL_020.md` реализовать сигналы:  
       - недостающие обязательные формы к моменту визита;  
       - просроченные формы (`expired`) с зависимыми записями;  
       - ревокации (`revoked`) для уже проведённых визитов.  
4.2.2. Создавать `AttentionItem`/Tasks с понятными типами и связями с пациентом/визитом/формой.

4.3. **Уважение SEC/RBAC при интеграции с UI и доменами.**  
4.3.1. Убедиться, что:  
       - врачи/админы видят только те формы, к которым имеют доступ по ролям и `clinic_id`;  
       - ни один внешний модуль (включая потенциальный AI‑анализ форм в будущем) не обходит RBAC‑слой.  
4.3.2. Описать, какие роли имеют доступ к каким типам форм (врачи, регистраторы, владелец/юристы).

---

### 5. Observe — логи, метрики и ПД

5.1. **Логирование ключевых операций Paperless.**  
5.1.1. В сервисах Paperless логировать:  
       - создание/выдачу/подписание/отзыв форм;  
       - изменения статусов и связей с визитами;  
       - с указанием `clinic_id`, `form_id`, `template_code`, `patient_id`/`booking_id` (ID, не ПД), инициатора, действия.  
5.1.2. Не включать в логи медицинские данные/ПД из содержимого формы.

5.2. **Метрики по статусам и качеству документооборота.**  
5.2.1. Ввести метрики:  
       - распределение форм по статусам (`draft`/`issued`/`signed`/`expired`/`revoked`);  
       - долю визитов без обязательных подписанных форм;  
       - среднее время от `issued` до `signed`.  
5.2.2. Связать эти метрики с OBS‑цепочками (`ARCH_DEV_OBS_CHAINS_023.md`) для цепи «запись → документы → визит».

5.3. **Политика ПД и будущая AI‑интеграция.**  
5.3.1. Зафиксировать на уровне кода/док‑комментариев, что:  
       - содержимое форм (`filled_data`) не используется напрямую в AI‑вызовах без `AiSanitizer` и tokenization (`ARCH_DEV_OMNI_POLICY_016.md`, `ARCH_DEV_AI_TOKENIZATION_025.md`);  
       - любые будущие AI‑сценарии вокруг форм требуют отдельного ARCH/DEV‑цикла.  
5.3.2. Проверить, что доступ к формам и их логам соответствует требованиям SEC/RBAC.

---

### 6. Stabilize — тесты, миграции и обновление артефактов

6.1. **Миграции данных и совместимость.**  
6.1.1. Подготовить миграции для `FormTemplate` и `FormInstance`:  
       - добавить/изменить таблицы и поля;  
       - перенести существующие данные в новые структуры, сохранив юридически значимые атрибуты (подписавшие/даты/состояния).  
6.1.2. Описать возможные расхождения/упрощения и зафиксировать их в `ARCH_AUDIT_NEXT.md` при необходимости.

6.2. **Покрыть Paperless‑модели и сервисы тестами.**  
6.2.1. Написать/обновить unit‑тесты для:  
       - `FormTemplate`/`FormInstance` моделей (создание, валидация, статусы);  
       - сервисов Paperless (создание/выдача/подписание/отмена/ревокация форм);  
       - проверок при смене статусов визита (если связаны напрямую).  
6.2.2. Добавить сценарные тесты:  
       - отсутствие обязательных форм при визите;  
       - просрочка форм и соответствующие Attention/Tasks;  
       - ревокация форм и её влияние на визиты.

6.3. **Синхронизация документации и GAPS.**  
6.3.1. После завершения реализации обновить:  
       - `DEV_PROMPTS_NEXT.md` — статус `DEV_PROMPT_PPR_MODEL_018`;  
       - `BACKEND_GAPS_Paperless_NEXT.md` — отметить закрытые/уточнённые пункты PPR‑2;  
       - QA‑документы по Paperless (если есть отдельные файлы);  
       - при необходимости `ARCH_AUDIT_NEXT.md` — описание новой модели Paperless и точек наблюдаемости.  
6.3.2. Убедиться, что новая модель Paperless не конфликтует с планами по `DEV_PROMPT_PPR_ESIGN_019` (e‑signature).

---

## Выполнено (@QA_ARCH / @DEV, 2026‑03‑20)

Ниже — фактически сданное по **PPR‑2 v1** (код + миграции + ключевые тесты). Дублирующий блок задач в середине файла (повтор заголовка с §1) оставлен как исторический; **источник правды по статусу** — этот раздел и `ARCH_DEV_PPR_MODEL_018.md` §«Статус реализации».

| Область | Сделано |
|--------|---------|
| **Модель / БД** | `DigitalFormSubmission.status`, `signed_at`, `expires_at`, audit‑поля; `DigitalFormTemplate.required_for_visit_completion`; `FormAuditEntry`; связь токена сабмишена; Alembic **`h4i5j6k7l8m9`** |
| **Домен** | `FormStatus` (+ безопасный разбор), `FormStatusService`, расширенный `FormsService` (выдача/подпись/expire/revoke, метрики, повторная выдача по ссылке) |
| **Booking** | `BookingCompletionService`: проверка обязательных форм **до** loyalty/ERP; задача **`PAPERLESS_REQUIRED_FORMS_MISSING`**; **SAVEPOINT** вокруг loyalty + ERP + перехода в `completed`; при ошибке ERP откат списания loyalty; ответы при сбое ERP с `loyalty_summary=None` |
| **API / админка** | Фильтры, revoke/cancel; типы и страница форм (по объёму v1) |
| **Тесты** | Unit/сервисные по формам и completion; в `tests/conftest.py` — напоминание про **`upgrade_test_db.py`** и миграции |

**Не закрыто как «полные фазы 1–6»:** синхронизация `DEV_PROMPTS_NEXT.md`, `BACKEND_GAPS_Paperless_NEXT.md` (кроме краткой отметки), полный набор OBS‑метрик по покрытию визитов, e2e‑цепочка Paperless↔Booking в CI.

---

## На потом (@QA_ARCH — предложения)

1. **Публичный шаг `in_progress` по токену** — явный эндпоинт/действие при открытии формы пациентом (аудит и метрики «issued → in_progress → signed» без опоры только на косвенные сигналы).
2. **Дашборд‑метрики покрытия визитов** — доработать §5.2 ARCH: доля визитов без полного набора обязательных подписанных форм, экспорт в OBS/Grafana, алерты для владельца клиники.
3. **Loyalty и flush** — зафиксировать инвариант: внутри savepoint нет «тихих» `flush` вне блока (при появлении новых вызовов loyalty пересмотреть границы транзакции); при необходимости — один явный commit‑policy в документации фасада.
4. **CI / тестовая БД** — job или документированный шаг: `alembic upgrade head` на БД из `DATABASE_URL_TEST` до pytest; сценарий **stamp** при уже существующих таблицах (`../../MIGRATION_UPGRADE.md`).
5. **Ревокация `signed` и влияние на уже завершённые визиты** — отдельные Tasks/Attention и бизнес‑правила (§4.2 TASKS), согласование с юридическим контуром.
6. **PWA / единый словарь статусов** — выровнять все экраны пациента под enum и пустые/ошибочные состояния по `TESTING_CANON` / TECH_PASSPORT.
7. **Полная миграция legacy‑данных** — если в проде остались строки без статуса: скрипт/backfill и мониторинг `UNKNOWN`/дефолтов.
8. **Синхронизация артефактов** — закрыть пункты 6.3.x: `DEV_PROMPTS_NEXT.md`, развернуть правки в `BACKEND_GAPS_Paperless_NEXT.md` (PPR‑2 → частично закрыт), при необходимости короткий QA_REPORT в `./`.

---

**Примечание:** в середине файла дублируется заголовок «ARCH_DEV_PPR_MODEL_018_TASKS» и второй полный набор секций §1–6 — при редактировании ориентироваться на **верхнюю** копию и на разделы **«Выполнено» / «На потом»** в конце.

---

