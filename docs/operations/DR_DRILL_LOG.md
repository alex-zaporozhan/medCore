# DR drill log

Append-only journal for restore drills (date, environment, result, owner).

Procedure: [DR_RUNBOOK.md](./DR_RUNBOOK.md). Optional CI: `.github/workflows/dr-restore-drill.yml` (`workflow_dispatch`).
