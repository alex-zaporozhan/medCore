# Backup schedule

Operator checklist: define retention, storage, and verification. Example script: `scripts/ops/backup_postgres.sh` (if present in repo).

Canonical DR context: [DR_RUNBOOK.md](./DR_RUNBOOK.md).
