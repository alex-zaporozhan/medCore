## ARCH_DEV_OBS_CHAINS_023_TASKS — сверх‑детализированные dev‑таски

> Связанная архитектура: `ARCH_DEV_OBS_CHAINS_023.md`  
> Связанный DEV_PROMPT: `DEV_PROMPT_OBS_CHAINS_023` (P1, OBS‑1/2, PERF‑1/2/3)

---

### 1. Understand — где сейчас есть/нет наблюдаемость по цепочкам

1.1. **Сопоставить OBS‑/PERF‑GAPS с целевыми цепочками.**  
1.1.1. Открыть `NONFUNCTIONAL_AUDIT_NEXT.md` (OBS‑1/2, PERF‑1/2/3) и все `BACKEND_GAPS_*_NEXT.md`, где есть пункты про наблюдаемость/перф.  
1.1.2. Для каждого такого пункта отнести его к одной из четырёх цепочек из `ARCH_DEV_OBS_CHAINS_023.md`:  
       - Booking → ERP/CRM/Loyalty;  
       - Omnichannel + AI;  
       - CRM + Attribution;  
       - Tasks & Attention.

1.2. **Зафиксировать текущие логи и метрики по этим цепочкам.**  
1.2.1. Для каждого домена (Booking, ERP, Loyalty, CRM, Omnichannel, Attribution, Tasks) посмотреть:  
       - какие структурированные логи уже есть (ключи, формат);  
       - какие метрики уже собираются (названия, лейблы).  
1.2.2. Составить краткую таблицу `цепочка → шаг → есть/нет логов → есть/нет метрик`.

---

### 2. Design‑to‑code — trace_id и схема метрик для цепочек

2.1. **Спроектировать единый `trace_id`/`correlation_id`.**  
2.1.1. Определить, как будет генерироваться и передаваться `trace_id`:  
       - на входе HTTP‑запроса (header `X-Trace-Id` или генерация при отсутствии);  
       - при постановке/обработке Celery‑тасок (передача в payload/контекст).  
2.1.2. Выбрать формат (`UUID`/строка) и зафиксировать его в `ARCH_DEV_OBS_CHAINS_023.md` (если нужно дополнение).

2.2. **Спроектировать набор метрик для каждой цепочки.**  
2.2.1. Для цепочки Booking → ERP/CRM/Loyalty определить:  
       - счётчики запусков/ошибок (`business_chain_booking_erp_total`, `..._errors_total`);  
       - гистограммы длительности общей цепочки и шагов (`..._duration_seconds`, `..._step_duration_seconds{step="erp"|"loyalty"|"prepare"|"crm"}`).  
2.2.2. Аналогично для Omnichannel+AI, CRM+Attribution, Tasks&Attention спроектировать имена и лейблы метрик по паттерну из ARCH‑документа.

---

### 3. Implement — внедрение trace_id в код

3.1. **Реализовать middleware/утилиту для trace_id.**  
3.1.1. В HTTP‑слое добавить middleware, который:  
       - берёт `X-Trace-Id` из входящего запроса или генерирует новый;  
       - сохраняет его в контекст (например, в `request.state` или глобальный контекст логгера);  
       - при необходимости добавляет `X-Trace-Id` в ответ.  
3.1.2. В инфраструктуре Celery:  
       - при постановке задач добавлять `trace_id` в payload (если есть в текущем контексте);  
       - при обработке задач извлекать `trace_id` и класть в контекст логгера/метрик.

3.2. **Прокинуть trace_id через ключевые сервисы.**  
3.2.1. В фасаде `BookingCompletionService`, ERP‑узле, Loyalty‑сервисах, CRM/Attribution‑сервисах, Omnichannel‑оркестраторе и Tasks/Attention‑слое:  
       - обеспечить доступ к `trace_id` из контекста;  
       - включать его в структурированные логи и, при необходимости, в DTO ошибок (там, где предусмотрено).

---

### 4. Implement — структурированные логи по шагам цепочек

4.1. **Добавить/нормализовать логи для цепочки Booking → ERP/CRM/Loyalty.**  
4.1.1. В `BookingCompletionService.complete_visit`:  
       - логировать `start` события с `trace_id`, `booking_id`, `clinic_id`, `from_status`, `target_status`;  
       - логировать шаги подготовки данных (`step="prepare"`), вызов ERP‑узла (`step="erp"`), вызов Loyalty (`step="loyalty"`), обновление CRM/Attribution (`step="crm"`), финальный исход (`success`/`error`).  
4.1.2. В `ErpVisitNodeService` и Loyalty‑сервисах:  
       - добавить логи `start`/`success`/`error` по шагам, включая `trace_id`, доменный контекст и коды ошибок.

4.2. **Добавить/нормализовать логи для Omnichannel + AI.**  
4.2.1. В Omnichannel‑входах (REST/WS): логировать получение сообщения с `trace_id`, `channel`, `direction`.  
4.2.2. В AI‑оркестраторе:  
       - логировать построение контекста (без ПД, только id/токены);  
       - логировать вызовы AI через SafeAiClient (`step="llm"`), включая исход/ошибки;  
       - логировать доменные действия, которые произвёл AI (создание задач, изменение записей и т.п.).

4.3. **Добавить/нормализовать логи для CRM + Attribution и Tasks & Attention.**  
4.3.1. В CRM‑сервисах: логировать ключевые переходы стадий лида с привязкой к источникам трафика и `trace_id`.  
4.3.2. В Tasks/Attention: логировать создание AttentionItem, задач и связей между ними (`trace_id`, доменный источник, тип сигнала/задачи).

---

### 5. Implement — метрики по цепочкам

5.1. **Ввести метрики для Booking → ERP/CRM/Loyalty.**  
5.1.1. В точках начала/завершения цепочки и ключевых шагов (`prepare`, `erp`, `loyalty`, `crm`):  
       - инкрементировать счётчики запусков/ошибок;  
       - измерять время выполнения шагов и всей цепочки (histogram).  
5.1.2. Убедиться, что имена и лейблы метрик следуют паттерну из ARCH‑документа.

5.2. **Ввести метрики для Omnichannel+AI, CRM+Attribution, Tasks&Attention.**  
5.2.1. Для Omnichannel+AI:  
       - количество AI‑запусков, ошибок AI, среднее время ответа LLM, долю fallback‑сценариев.  
5.2.2. Для CRM+Attribution:  
       - количество успешных/неуспешных построений отчётов, латентность;  
       - базовые ROI‑метрики, если не покрыты ERP‑отчётным слоем.  
5.2.3. Для Tasks&Attention:  
       - количество сигналов по категориям, количество задач по источникам (ручные/AI/system), среднее время до закрытия задач.

5.3. **Настроить базовые SLO/алерты (если есть мониторинг).**  
5.3.1. На основе новых метрик задать пороги:  
       - долю успешных завершений цепочек;  
       - максимальное P95 времени завершения визита/AI‑ответа/отчёта;  
       - пороги по частоте ошибок в цепочках.

---

### 6. Integrate — связь с Tasks/Attention и GAPS

6.1. **Интегрировать критичные сбои с Tasks/Attention.**  
6.1.1. В точках критичных ошибок по цепочкам (невозможность завершить визит, массовые ошибки AI/Omnichannel, сбои отчётности) вызвать генерацию `AttentionItem`/Task по шаблону `DEV_PROMPT_TASKS_MODEL_020`, включая `trace_id` и доменный контекст.  
6.1.2. Убедиться, что по Attention можно вернуться к полному пути цепочки через поиск по `trace_id` в логах.

6.2. **Синхронизировать OBS‑GAPS и документацию.**  
6.2.1. После внедрения изменений обновить `NONFUNCTIONAL_AUDIT_NEXT.md`, пометив закрытые OBS/PERF‑пункты.  

6.2.2. При необходимости дополнить `ARCH_DECISIONS_NEXT.md` уточнёнными принципами наблюдаемости (trace_id, обязательные метрики по цепочкам).

---

### 7. Later / улучшения «на потом»

7.1. **Booking → ERP/CRM/Loyalty.**  
7.1.1. Уточнить и, при необходимости, расширить шаг `prepare` в `BookingCompletionService.complete_visit`, чтобы в логах и метриках отражались ключевые агрегаты (суммы по услугам/оплатам) без ПД.  
7.1.2. При появлении отдельного CRM/Attribution‑шага в завершении визита добавить для него явные логи (`chain="booking_to_erp"`, `step="crm"`) и, при необходимости, отдельные step‑метрики.  

7.2. **Omnichannel + AI.**  
7.2.1. Если в следующих версиях появятся дополнительные шаги оркестрации (отдельная нормализация текста, обогащение доменными данными и т.п.), расширить `business_chain_omni_ai_step_duration_seconds` новыми значениями `step`, сохранив совместимость с текущими графиками.  
7.2.2. Добавить более детализированные доменные логи действий AI‑агента (создание задач, изменение записей и др.) с `trace_id` и устойчивыми кодами операций, чтобы по одному `trace_id` можно было восстановить не только путь сообщений, но и все связанные изменения в домене.  

7.3. **CRM + Attribution.**  
7.3.1. При расширении CRM‑воронки и отчётности добавить структурированные логи ключевых переходов стадий лида с привязкой к источникам трафика/кампаниям и `trace_id`, не нарушая уже заведённые метрики `business_chain_crm_attribution_*`.  
7.3.2. При появлении тяжёлых/многопроходных отчётов в attribution‑слое ввести дополнительные step‑метрики (парсинг данных, агрегация, рендеринг) поверх базовой `business_chain_crm_attribution_duration_seconds`.  

7.4. **Tasks & Attention.**  
7.4.1. Стандартизировать включение `trace_id` в AttentionItem/Task (например, в отдельном поле/метаданных), чтобы поиск по `trace_id` не зависел от текстовых описаний.  
7.4.2. При расширении типов Attention‑сигналов убедиться, что для новых категорий также заведены категория‑ориентированные метрики (количество сигналов, время до закрытия) и логи с единым форматом (`kind`, `source`, `trace_id`).  

7.5. **SLO и алерты.**  
7.5.1. На уровне мониторинга (Prometheus/Grafana/Alertmanager) оформить SLO/алерты по цепочным метрикам (`business_chain_*`): P95 длительности, доля успешных завершений, частота ошибок по типам; поддерживать конфигурации так, чтобы добавление новых шагов не ломало существующие дашборды.
