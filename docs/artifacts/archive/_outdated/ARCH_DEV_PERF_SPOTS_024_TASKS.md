## ARCH_DEV_PERF_SPOTS_024_TASKS — сверх‑детализированные dev‑таски

> Связанная архитектура: `ARCH_DEV_PERF_SPOTS_024.md`  
> Связанный DEV_PROMPT: `DEV_PROMPT_PERF_SPOTS_024` (P2, PERF‑1/2/3)

---

### 1. Understand — сбор фактов по перфу до оптимизаций

1.1. **Собрать список конкретных perf‑проблем.**  
1.1.1. На основе `NONFUNCTIONAL_AUDIT_NEXT.md` (PERF‑1/2/3), текущих метрик и логов (после `DEV_PROMPT_OBS_CHAINS_023`) сформировать перечень проблем не в общих формулировках, а в виде: `endpoint`, `запрос к БД`, `компонент фронта`.  
1.1.2. Для каждой проблемы зафиксировать, к какой цепочке она относится (CRM Kanban, ERP‑отчёты, Omnichannel/AI).

1.2. **Собрать измерения по каждой проблеме.**  
1.2.1. Для каждого perf spot зафиксировать:  
       - P50/P90/P95/P99 время ответа;  
       - объём данных (количество записей, размер payload);  
       - частоту вызовов.  
1.2.2. Использовать существующие метрики `business_chain_*` и endpoint‑метрики из OBS‑слоя (или добавить недостающие).

1.3. **Сопоставить с архитектурой и инвариантами.**  
1.3.1. По `ARCH_DECISIONS_NEXT.md`, `ARCH_CRM_NEXT.md`, `ARCH_ERP_NEXT.md`, `ARCH_OMNICHANNEL_NEXT.md` проверить, какие ограничения/принципы нельзя нарушать при оптимизациях (multi‑tenant, RBAC, AI‑policy и т.п.).  
1.3.2. Отдельно отметить места, где текущий код уже нарушает эти принципы во имя перфа (если такие найдутся).

---

### 2. Design‑to‑code — план оптимизаций и технические приёмы

2.1. **Классифицировать perf‑проблемы по типам.**  
2.1.1. Для каждого perf spot определить его класс:  
       - данные/запросы к БД (N+1, отсутствие индексов, тяжёлые join’ы);  
       - бизнес‑логика/алгоритмы (неоптимальные циклы, перерасчёт агрегатов);  
       - сетевые/AI‑вызовы (длинные цепочки, нет тайм‑аутов/ретраев);  
       - фронтенд (огромные списки без виртуализации, тяжёлые JSON).  
2.1.2. Зафиксировать список spot’ов по классам с ссылками на код/запросы.

2.2. **Выбрать приёмы оптимизации для каждого spot.**  
2.2.1. По разделу 3.3 `ARCH_DEV_PERF_SPOTS_024.md` выбрать 1–2 подходящих приёма для каждого spot: индексы, перепроектирование запросов, кэш/предагрегация, асинхронность, фронтовые оптимизации и т.п.  
2.2.2. Оценить усилия, риски и влияние на архитектурные инварианты для каждого выбранного приёма.

2.3. **Составить поэтапный план внедрения.**  
2.3.1. Приоритизировать perf spot’ы по: бизнес‑важности (CRM Kanban/владельческие отчёты/Omnichannel), тяжести проблемы и сложности исправления.  
2.3.2. Сформировать список шагов «микро‑эпиков»/тасок с понятными критериями успешности (целевые латентности/нагрузки).

---

### 3. Implement — CRM Kanban и списки

3.1. **Оптимизировать выборки для Kanban и крупных списков CRM.**  
3.1.1. Проанализировать SQL/ORM‑запросы, лежащие в основе `GET /crm/leads/kanban` и других тяжёлых CRM‑эндпоинтов: убрать N+1, лишние join’ы, неиспользуемые поля.  
3.1.2. При необходимости вынести «Kanban projection» — облегчённое представление лида с минимальными полями, используемыми на доске.

3.2. **Ввести/уточнить индексы и пагинацию.**  
3.2.1. На таблицах CRM‑лидов/сущностей добавить составные индексы по частым фильтрам/сортировкам (clinic_id, status, assignee, date и др.).  
3.2.2. Внедрить строгие лимиты и пагинацию (страничную или курсорную) для колонок Kanban и тяжёлых списков.

3.3. **Оптимизировать фронтенд CRM.**  
3.3.1. В компоненте Kanban и CRM‑списках внедрить виртуализацию отображения (рендер только видимых элементов), мемоизацию и разделение компонентов по колонкам.  
3.3.2. Убедиться, что payload от backend не содержит избыточных/тяжёлых полей для первичной отрисовки доски.

---

### 4. Implement — ERP/Attribution отчёты и Omnichannel/AI

4.1. **Оптимизировать ERP/Attribution отчёты.**  
4.1.1. Для тяжёлых отчётных эндпоинтов (см. `ARCH_DEV_ERP_REPORTS_012.md`) реализовать или доработать слой предагрегированных данных (дневные/клиник‑/кампейн‑агрегаты).  
4.1.2. Обновить отчётные эндпоинты так, чтобы они работали поверх предагрегированных таблиц/представлений, а не по «сырым» транзакциям за большие периоды.

4.2. **Ограничить «тяжёлые» запросы и добавить фоновые пересчёты.**  
4.2.1. В отчётных API ввести ограничения на периоды/объёмы данных (запрет запросов «за всё время» без фильтров), возвращая понятные ошибки/предупреждения.  
4.2.2. Настроить фоновые задачи (Celery/cron) для регулярного пересчёта агрегатов, используемых в ключевых владельческих отчётах.

4.3. **Оптимизировать Omnichannel/AI‑цепочки.**  
4.3.1. С помощью метрик и трейсинга выявить самые «долгие» AI‑цепочки (по `ARCH_DEV_OMNI_REGISTRY_015.md`): последовательные LLM‑вызовы, отсутствие тайм‑аутов.  
4.3.2. Где возможно, ввести параллелизацию шагов, жёсткие тайм‑ауты и fallback‑логики (упрощённые ответы, частичная деградация) без нарушения SEC/PD‑политики.

---

### 5. Observe — дополнительные метрики и валидация перф‑изменений

5.1. **Расширить точечные перф‑метрики.**  
5.1.1. Для ключевых perf spot’ов добавить или уточнить метрики:  
       - `endpoint_duration_seconds{path="..."}` по CRM/ERP/Omni эндпоинтам;  
       - размер выборок/ответов;  
       - перцентильные латентности.  
5.1.2. Связать эти метрики с бизнес‑цепочками `business_chain_*`, чтобы видеть влияние оптимизаций на end‑to‑end поток.

5.2. **Провести сравнение «до/после».**  
5.2.1. Для каждого оптимизированного spot’а провести замеры до и после изменений, зафиксировать результаты в технической документации или комментариях к таскам.  
5.2.2. Проверить, что улучшения не достигнуты ценой нарушения архитектурных инвариантов (multi‑tenant, RBAC, корректность бизнес‑результатов).

5.3. **Добавить нагрузочные/интеграционные тесты.**  
5.3.1. В разумных пределах внедрить нагрузочные/интеграционные тесты для ключевых эндпоинтов (CRM Kanban, отчёты, Omnichannel), которые можно запускать как минимум в CI/стейджинг‑среде.  
5.3.2. Зафиксировать сценарии и целевые пороги (SLO/SLA), на которые эти тесты должны ориентироваться.

---

### 6. Stabilize — документация, GAPS и оставшиеся perf‑риски

6.1. **Обновить нефункциональный аудит и GAPS.**  
6.1.1. В `NONFUNCTIONAL_AUDIT_NEXT.md` отметить PERF‑GAPS, которые были смягчены или закрыты в рамках DEV_PROMPT_PERF_SPOTS_024.  
6.1.2. При необходимости обновить связанные `BACKEND_GAPS_*_NEXT.md` и `FRONTEND_GAPS_*_NEXT.md` (особенно CRM/ERP/Omnichannel).

6.2. **Зафиксировать оставшиеся perf‑проблемы.**  
6.2.1. Для spot’ов, которые было решено не трогать в текущей итерации (слишком дорогие/низкий приоритет), явно задокументировать статус и причины.  
6.2.2. Приоритизировать эти точки для будущих релизов и, при необходимости, завести отдельные задачи/epic’и.

6.3. **Коммуникация с владельцами доменов.**  
6.3.1. Передать владельцам CRM/ERP/Omnichannel краткий отчёт: какие перф‑ограничения теперь считаются «границами разумного» (объёмы данных, ожидания по времени ответа).  
6.3.2. Обновить соответствующие разделы `ARCH_DECISIONS_NEXT.md` (если требуется) отражением принятых перф‑решений и ограничений.

## ARCH_DEV_PERF_SPOTS_024_TASKS — сверх‑детализированные dev‑таски

> Связанная архитектура: `ARCH_DEV_PERF_SPOTS_024.md`  
> Связанный DEV_PROMPT: `DEV_PROMPT_PERF_SPOTS_024` (P2, PERF‑1/2/3)

---

### 1. Understand — где именно болит по производительности

1.1. **Собрать текущие факты по PERF‑GAPS.**  
1.1.1. По `NONFUNCTIONAL_AUDIT_NEXT.md` (PERF‑1/2/3) и `BACKEND_GAPS_*_NEXT.md` выписать:  
       - какие области уже помечены как проблемные (CRM Kanban, ERP/Attribution отчёты, Omnichannel/AI и др.);  
       - какие симптомы там зафиксированы (долгое время ответа, тайм‑ауты, падения).  
1.1.2. Зафиксировать список конкретных эндпоинтов/страниц/цепочек, упомянутых в аудите.

1.2. **Использовать существующую наблюдаемость для подтверждения проблем.**  
1.2.1. На основе метрик/логов из `ARCH_DEV_OBS_CHAINS_023.md` (и фактической реализации OBS):  
       - собрать данные по времени выполнения и ошибкам ключевых бизнес‑цепочек (Booking → ERP/CRM/Loyalty, Omnichannel+AI, CRM+Attribution, Tasks&Attention).  
1.2.2. Определить, какие конкретные участки (эндпоинты, запросы к БД, фронтовые страницы) дают наибольший вклад в 90/95/99‑й перцентили latency.

1.3. **Инвентаризировать конкретные эндпоинты и запросы.**  
1.3.1. Для CRM Kanban/списков:  
       - найти используемые API (`/crm/leads/kanban`, списки задач/контактов и т.п.);  
       - зафиксировать текущие запросы к БД (ORM/SQL, join’ы, фильтры, сортировки).  
1.3.2. Для ERP/Attribution отчётов:  
       - определить отчётные эндпоинты (`admin_reports`, ERP/Attribution API) с тяжелыми агрегациями;  
       - выписать используемые запросы/агрегирующие представления, их параметры и объёмы данных.  
1.3.3. Для Omnichannel/AI:  
       - выделить цепочки AI‑вызовов (количество LLM‑шагов, последовательность, параллелизм/отсутствие).  

1.4. **Сформировать список perf‑spots.**  
1.4.1. На основе 1.1–1.3 составить явный список perf‑spots в виде:  
       - `perf_spot_id`, «что болит» (endpoint/query/component), симптомы, данные метрик/логов.  
1.4.2. Присвоить каждому spot’у приоритет (impact × frequency × effort) для дальнейшей работы.

---

### 2. Design‑to‑code — подходы к оптимизации для каждого perf‑spot

2.1. **Классифицировать типы проблем.**  
2.1.1. Для каждого perf‑spot определить его тип (см. ARCH‑док):  
       - данные/запросы к БД (N+1, отсутствие индексов, тяжёлые join’ы);  
       - бизнес‑логика/алгоритмы (избыточные циклы, пересчёт агрегатов on‑the‑fly);  
       - сетевые вызовы/AI (длинные цепочки без параллелизма/тайм‑аутов);  
       - фронтенд (рендеринг без виртуализации, тяжёлые payload’ы).  
2.1.2. Перенести классификацию в таблицу/структуру (в коде или в docs) для отслеживания.

2.2. **Выбрать архитектурные приёмы для каждого класса.**  
2.2.1. На уровне дизайна (не кода) выбрать для каждого вида проблемы 1–2 подходящих приёма:  
       - для БД: индексы/перепроектирование запросов, облегчённые проекции, пагинация;  
       - для логики: мемоизация, вынесение тяжёлых расчётов в фон, кэш;  
       - для AI/сетевых цепочек: параллелизация, асинхронность, тайм‑ауты, graceful‑fallback;  
       - для фронта: виртуализация списков, уменьшение payload’ов, разбиение компонентов.  
2.2.2. Задокументировать выбранный приём для каждого perf‑spot (чтобы в реализации не «плавать»).

2.3. **Согласовать границы «нормы» (SLO/SLA) по ключевым областям.**  
2.3.1. Совместно с бизнесом/продуктом (по материалам `ARCH_DECISIONS_NEXT.md`):  
       - зафиксировать ориентиры по времени ответа и объёмам данных для:  
         - CRM Kanban (максимальное число лидов/колонку, время первого рендера);  
         - владельческих отчётов ERP/Attribution (время построения/периоды);  
         - Omnichannel/AI‑ответов (максимально допустимая задержка).  
2.3.2. Использовать эти ориентиры как критерии «готово» для оптимизаций.

---

### 3. Implement — точечные оптимизации CRM/ERP/Omni (backend)

3.1. **CRM Kanban: оптимизация запросов и проекций.**  
3.1.1. В API/репозиториях CRM Kanban:  
       - оптимизировать запросы:  
         - добавить/уточнить индексы по частым фильтрам (`clinic_id`, статус, ответственный, дата);  
         - выделить «лёгкую» проекцию лида (минимальный набор полей для Kanban, без тяжёлых join’ов).  
3.1.2. Ввести явную пагинацию/лимиты по колонкам:  
       - ограничить число лидов, отдаваемых за один запрос;  
       - использовать курсорную/страничную пагинацию для длинных колонок.

3.2. **ERP/Attribution отчёты: предагрегация и лимиты.**  
3.2.1. На отчётных эндпоинтах ERP/Attribution:  
       - вынести тяжёлую агрегацию на слой предагрегированных представлений/таблиц (по дням/клиникам/источникам трафика);  
       - реализовать фоновые задачи (Celery/cron) для регулярного пересчёта агрегатов.  
3.2.2. В API отчётов:  
       - ограничить возможность запросов «за всё время» без фильтров;  
       - при необходимости ввести «сэнд‑боксы» (макс. период/объём для онлайн‑запроса).

3.3. **Omnichannel/AI: оптимизация цепочек.**  
3.3.1. В Omnichannel AI Orchestrator’е:  
       - измерить время выполнения каждого шага в AI‑цепочках;  
       - там, где возможно, перевести последовательные шаги в параллельное выполнение;  
       - добавить тайм‑ауты и fallback‑варианты (например, сокращённый ответ вместо полного анализа).  
3.3.2. Рассмотреть вынесение особо тяжёлых AI‑операций в асинхронный режим:  
       - мгновенный ответ UI с сообщением о том, что «анализ готовится»;  
       - последующая доставка результата через Omni/Tasks.

3.4. **Прочие backend‑оптимизации по конкретным spot’ам.**  
3.4.1. Для каждого зафиксированного perf‑spot’а реализовать выбранный приём (индекс/перепроектирование/кэш/фон):  
       - с максимально локальными изменениями, не трогающими доменную модель/инварианты.  
3.4.2. При необходимости добавить кэширование (с оговорённым временем жизни) для тяжёлых, но редко меняющихся запросов.

---

### 4. Implement — фронтенд‑оптимизации (CRM/отчёты/Omni)

4.1. **CRM Kanban и тяжёлые списки.**  
4.1.1. В CRM‑фронте (Kanban, большие списки):  
       - внедрить виртуализацию списка/колонок, чтобы в DOM одновременно рендерилась только видимая часть;  
       - разделить компоненты на более мелкие (мемоизация, `React.memo`/аналог) для снижения перерендера.  
4.1.2. Передавать в Kanban‑компонент только необходимые поля (согласованно с облегчённой проекцией на backend).

4.2. **Фронтовой слой отчётов ERP/Attribution.**  
4.2.1. В UI отчётов:  
       - использовать ленивую загрузку (progress состояние, подзагрузка деталей по клику);  
       - избегать отображения огромных таблиц/графиков без фильтров (подсказывать пользователю ограничить период/размер выборки).  
4.2.2. При необходимости использовать клиентский кэш/мемоизацию для часто повторяющихся запросов с одинаковыми параметрами.

4.3. **Omnichannel/AI‑UI.**  
4.3.1. В Omnichannel UI:  
       - обеспечить, чтобы долгие AI‑операции не блокировали основной интерфейс (использовать loading‑состояния, неблокирующие спиннеры, push‑модель обновления);  
       - при необходимости сократить объём данных, передаваемых в AI‑панель (согласованно с backend).

---

### 5. Observe — измерение до/после, метрики и документация

5.1. **Сделать baseline измерений до оптимизаций.**  
5.1.1. Для каждого perf‑spot’а зафиксировать:  
       - P50/P90/P95/P99 времени ответа (по метрикам/трейсам);  
       - объёмы данных (количество строк/объектов в выборках, размер JSON‑ответов);  
       - частоту вызовов.  
5.1.2. Сохранить эти baseline‑значения в виде отчёта/раздела docs (для сравнения после оптимизаций).

5.2. **Обновить/расширить метрики по конкретным точкам.**  
5.2.1. При необходимости добавить специфические метрики:  
       - `endpoint_duration_seconds{path="/crm/leads/kanban"}`;  
       - счётчики размера выборок/ответов;  
       - время построения ключевых отчётов;  
       - latency AI‑цепочек в Omnichannel.  
5.2.2. Интегрировать эти метрики с уже существующими OBS‑цепочками, чтобы видеть эффект оптимизаций в контексте бизнес‑процессов.

5.3. **Снять метрики после оптимизаций и зафиксировать результат.**  
5.3.1. После внедрения оптимизаций повторно снять метрики по тем же сценариям, что и в baseline:  
       - сравнить времена, объёмы и ошибки до/после;  
       - задокументировать улучшения (или их отсутствие).  
5.3.2. Если ожидаемый эффект не достигнут, зафиксировать это и, при необходимости, спланировать дополнительные шаги или принять ограничение как временное.

---

### 6. Stabilize — регрессия, GAPS и последующие шаги

6.1. **Регрессионные и нагрузочные тесты.**  
6.1.1. Разработать/обновить нагрузочные/интеграционные тесты для:  
       - CRM Kanban (много лидов, фильтры/перетаскивания);  
       - ключевых ERP/Attribution отчётов (на больших периодах/клиниках);  
       - Omnichannel/AI‑цепочек (с эмуляцией реальных нагрузок).  
6.1.2. Включить эти тесты в регулярный перф‑контур (по возможности).

6.2. **Синхронизация с нефункциональными GAPS.**  
6.2.1. После внедрения оптимизаций обновить:  
       - `NONFUNCTIONAL_AUDIT_NEXT.md` — отметить закрытые/смягчённые PERF‑GAPS;  
       - при необходимости соответствующие разделы в `BACKEND_GAPS_*_NEXT.md`, `FRONTEND_GAPS_*_NEXT.md`.  
6.2.2. Остаточные/новые perf‑проблемы явно задокументировать и приоритизировать для следующих релизов.

6.3. **Связь с другими DEV_PROMPT и архитектурными решениями.**  
6.3.1. Зафиксировать, какие оптимизации зависят от/поддерживают другие DEV_PROMPT (CRM_EVENTS/CRM_MONEY, ERP_REPORTS, OMNI_REGISTRY, OBS_CHAINS и т.п.).  
6.3.2. Обновить (при необходимости) `ARCH_AUDIT_NEXT.md`/`ARCH_DECISIONS_NEXT.md` решениями по перфу (выбор подходов к кэшу, предагрегации, асинхронности).

---

## Выполнено (v1, 2026-03 — @DEV / зафиксировано @QA_ARCH)

- **§3 CRM Kanban (частично):** облегчённая проекция лидов (`projection=kanban`, `LeadKanbanCardDto`), ORM `load_only`, уменьшенный JSON на доску.
- **§3 пагинация / запросы:** один SQL на список + total (`COUNT(*) OVER()`), убран дублирующий `COUNT` в роутере.
- **§4 ERP (частично):** лимит длины периода отчётов (366 дней), расширенное сообщение об ошибке для пользователя.
- **§5 Observe (частично):** HTTP latency с разрезом по **`status_class`** и стабильным **`path`**; счётчик запросов списка CRM по **`projection`**.
- **Тесты:** см. `tests/core/test_metrics_path.py`, `tests/api/test_admin_crm.py`, `tests/api/test_admin_reports_erp_endpoints.py`, `tests/services/test_lead_crm.py`.
- **Архитектура:** статус сведён в **`ARCH_DEV_PERF_SPOTS_024.md` §8**.

## На потом (предложения @QA_ARCH)

- **Глубокий перф (Engine L2):** единый план — **`ARCH_PERF_ENGINE_L2_DEEP_2026.md`** (предагрегаты ERP, Celery, виртуализация списков, курсор по колонкам Kanban и др.).
- **Витрины ERP payroll / materials / ROI (эпик 026):** **`ARCH_DEV_ERP_VITRINES_026.md`**, **`ARCH_DEV_ERP_VITRINES_026_TASKS.md`**.
- **§1 Understand / §5.2 baseline:** зафиксировать P50/P90/P95/P99 по ключевым эндпоинтам до/после v1 в артефакте или в комментарии к релизу.
- **§6.1 GAPS:** обновить `NONFUNCTIONAL_AUDIT_NEXT.md` и при необходимости `BACKEND_GAPS_*` / `FRONTEND_GAPS_*` по PERF‑зонам CRM/ERP/Omni.
- **§5.1.2 связка с цепочками:** в Grafana/дашбордах согласовать `http_request_duration_seconds` с `business_chain_*` (общий контекст сценария без взрыва кардинальности).
- **§5.3 / §6.1 нагрузочные тесты:** контур для Kanban, ERP отчётов, Omni (staging/CI по возможности).
- **Ops:** проверить алерты Prometheus на **`http_request_duration_seconds`** — добавлен лейбл **`status_class`** (обновить записи правил).
- **§4.3 Omni / AI:** параллелизация шагов, тайм‑ауты, деградация — по `ARCH_DEV_OMNI_REGISTRY_015.md` (можно отдельным промптом после L2 ERP).

---

