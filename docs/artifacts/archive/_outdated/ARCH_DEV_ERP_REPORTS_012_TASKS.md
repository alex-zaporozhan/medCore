## ARCH_DEV_ERP_REPORTS_012_TASKS — сверх‑детализированные dev‑таски

> Связанная архитектура: `ARCH_DEV_ERP_REPORTS_012.md`  
> Связанный DEV_PROMPT: `DEV_PROMPT_ERP_REPORTS_012` (P2, ERP‑3, ATT‑2)  
> **Перф / витрины ERP (Engine L2, эпик 026):** `ARCH_DEV_ERP_VITRINES_026.md`, `ARCH_DEV_ERP_VITRINES_026_TASKS.md` — отдельный план на таблицы предагрегатов и Celery для `payroll-by-period`, `materials-by-period`, `roi-by-source`.

---

### 1. Understand — текущие отчёты и их проблемы

1.1. **Проанализировать существующие отчётные эндпоинты.**  
1.1.1. В `src/api/v1/routers/admin_reports.py` (и смежных роутерах) найти все текущие отчётные эндпоинты, связанные с ERP/финансами/атрибуцией.  
1.1.2. Для каждого эндпоинта зафиксировать: URL, параметры, возвращаемую структуру данных.

1.2. **Посмотреть, на чём они основаны.**  
1.2.1. Для каждого отчётного эндпоинта найти соответствующие ORM‑запросы/репозитории/SQL (или сервисы), которые он использует.  
1.2.2. Отдельно отметить места, где:  
       - явно реализуется сложная бизнес‑логика (перерасчёт сумм, статусов, обязательств) прямо в запросе;  
       - логика дублирует поведение ERP‑узла или связки ERP↔Loyalty.

1.3. **Сопоставить с GAPS и аудиторскими замечаниями.**  
1.3.1. Открыть `BACKEND_GAPS_ERP_NEXT.md` (ERP‑3), `BACKEND_GAPS_Attribution_NEXT.md` (ATT‑2) и `NONFUNCTIONAL_AUDIT_NEXT.md` (PERF‑2, OBS‑1/2).  
1.3.2. Для каждого gap указать, какие из найденных отчётных эндпоинтов/запросов к нему относятся (медленные, дублирующие логику, слабо наблюдаемые).  

---

### 2. Design‑to‑code — проектирование отчётных представлений

2.1. **Спроектировать базовые представления поверх ERP‑узла.**  
2.1.1. На основе `ARCH_DEV_ERP_REPORTS_012.md` и уже реализованного `ErpVisitNodeService`/`ErpLoyaltyService` определить, какие «сырые» таблицы/регистры доступны: финтранзакции по визитам, начисления ЗП, склад, обязательства.  
2.1.2. Спроектировать структуры для:  
       - `ErpVisitRevenueView`;  
       - `ErpVisitPayrollView`;  
       - `ErpVisitInventoryView`;  
       - `ErpLoyaltyObligationsView`;  
       - `ErpAttributionRevenueView` (выручка + источники трафика/кампании).

2.2. **Выбрать реализацию (view vs ORM‑запросы).**  
2.2.1. Совместно с @ARCH/@DBA решить, реализовывать ли эти представления как:  
       - SQL‑view + ORM‑модели;  
       - или специализированные репозитории с хорошо структурированными запросами.  
2.2.2. Зафиксировать этот выбор в `ARCH_DEV_ERP_REPORTS_012.md` перед изменениями.

2.3. **Спроектировать привязку к Attribution.**  
2.3.1. На основе `ARCH_ATTRIBUTION_NEXT.md` определить, как визиты/ERP‑записи связаны с источниками трафика/кампаниями.  
2.3.2. Для `ErpVisitRevenueView` и/или `ErpAttributionRevenueView` спроектировать поля `traffic_source_id`/`campaign_id` и схему join’ов, опираясь на существующие связи Booking ↔ LeadCard ↔ Attribution (v1: ORM‑репозитории без SQL‑VIEW; SQL/материализованные VIEW — в будущих улучшениях).  

---

### 3. Implement — реализация отчётного слоя и API

3.1. **Реализовать отчётные представления в коде.**  
3.1.1. Создать ORM‑модели/репозитории для:  
       - `ErpVisitRevenueView`;  
       - `ErpVisitPayrollView`;  
       - `ErpVisitInventoryView`;  
       - `ErpLoyaltyObligationsView`;  
       - `ErpAttributionRevenueView`.  
3.1.2. Убедиться, что они используют только данные, сформированные через ERP‑узел и связку ERP↔Loyalty (без собственной «бизнес‑логики» перерасчёта).

3.2. **Перевести существующие отчёты на новые представления.**  
3.2.1. В `admin_reports` и смежных роутерах для каждого найденного отчёта (из шага 1.1):  
       - заменить старые ORM‑запросы на обращения к новым представлениям/репозиториям;  
       - удалить из эндпоинтов дублирующую бизнес‑логику (оставить только агрегацию/фильтрацию).  
3.2.2. Для ещё не реализованных, но описанных в ARCH‑документе отчётов — добавить новые эндпоинты (`revenue-by-period`, `payroll-by-period`, `materials-by-period`, `loyalty-obligations`, `roi-by-source`) с контрактами из `ARCH_DEV_ERP_REPORTS_012.md`.

3.3. **Увязать отчётный слой с Attribution.**  
3.3.1. В представлениях выручки (по визитам и по источникам) добавить поля и join’ы для связи с источниками трафика и кампаниями.  
3.3.2. В эндпоинте `roi-by-source` реализовать агрегацию по источникам/кампаниям и периодам, используя `ErpAttributionRevenueView`.

---

### 4. Integrate — RBAC, фильтрация по клинике и UX‑контракты

4.1. **Применить RBAC к отчётным эндпоинтам.**  
4.1.1. Для всех отчётных эндпоинтов установить `require_permissions(...)` по матрице из `ARCH_DEV_SEC_RBAC_022.md` (например, `erp.owner_reports.read`, `attribution.reports.read`).  
4.1.2. Проверить, что роли с меньшим уровнем доступа (doctor, registrar и т.п.) не видят владельческие отчёты, если это не предусмотрено.

4.2. **Обеспечить фильтрацию по `clinic_id`.**  
4.2.1. В запросах к отчётным представлениям убедиться, что данные всегда фильтруются по `clinic_id`, доступному текущему пользователю/контексту.  
4.2.2. При необходимости добавить вспомогательные функции/утилиты для безопасного применения фильтра клиники во всех отчётных запросах.

4.3. **Уточнить и стабилизировать контракты API под фронт.**  
4.3.1. Сверить возвращаемые структуры отчётных эндпоинтов с фронтовыми DEV_PROMPTS/`FRONTEND_GAPS_*_NEXT.md` (если там уже описаны ожидания).  
4.3.2. При необходимости скорректировать форму ответов (поля, типы, агрегаты) так, чтобы фронту было удобно строить владельские отчётные экраны.

---

### 5. Observe — производительность и наблюдаемость отчётов

5.1. **Оптимизировать запросы и добавить индексы.**  
5.1.1. Для тяжёлых отчётов (большие периоды, разрезы по клиникам/кампаниям) замерить время выполнения запросов на приближённых к бою данных.  
5.1.2. Добавить необходимые индексы по ключевым полям фильтрации (дата, `clinic_id`, `doctor_id`, `service_id`, `traffic_source_id`, `campaign_id`).  
5.1.3. При необходимости ввести агрегирующие таблицы или материализованные представления, которые будут обновляться по расписанию или триггерам.

5.2. **Добавить логи и метрики для отчётных эндпоинтов.**  
5.2.1. Логировать параметры запросов (агрегировано), время выполнения и коды ответов.  
5.2.2. Добавить метрики:  
       - латентность по каждому отчётному эндпоинту;  
       - частоту вызовов;  
       - частоту ошибок/таймаутов.  
5.2.3. Интегрировать эти метрики с общей OBS‑системой (`DEV_PROMPT_OBS_CHAINS_023`, PERF‑GAPS).

---

### 6. Stabilize — тестирование и обновление артефактов

6.1. **Покрыть отчётный слой тестами.**  
6.1.1. Написать/обновить тесты для репозиториев/представлений:  
       - проверка корректности агрегатов на небольших, но репрезентативных наборах данных;  
       - проверка фильтров по клинике, врачам, услугам, источникам трафика.  
6.1.2. Добавить тесты на edge‑кейсы: пустые периоды, отсутствие данных, недопустимые параметры.

6.2. **Проверить RBAC и отказоустойчивость.**  
6.2.1. Написать тесты на доступ к отчётным эндпоинтам:  
       - роли с нужными правами видят отчёты (2xx);  
       - роли без прав получают 403/ошибку доступа.  
6.2.2. Проверить поведение при ошибках БД/таймаутах (корректные коды ошибок, отсутствие утечки внутренних деталей).

6.3. **Синхронизировать документацию и GAPS.**  
6.3.1. Обновить `DEV_PROMPTS_NEXT.md`, указав статус `DEV_PROMPT_ERP_REPORTS_012` и кратко зафиксировав, что:  
       - базовый ERP‑отчётный слой реализован поверх `ErpReportsRepository` (ORM‑репозитории вместо SQL‑VIEW в v1);  
       - добавлены эндпоинты `revenue-by-period`, `payroll-by-period`, `materials-by-period`, `loyalty-obligations`, `roi-by-source` с RBAC и фильтрацией по `clinic_id`;  
       - введены метрики `erp_reports_requests_total` для наблюдаемости.  
6.3.2. В `BACKEND_GAPS_ERP_NEXT.md` и `BACKEND_GAPS_Attribution_NEXT.md` отметить закрытые/частично закрытые пункты по ERP‑3 и ATT‑2.  
6.3.3. При необходимости дополнить `ARCH_AUDIT_NEXT.md` описанием новой отчётной схемы и мест, где смотреть ключевые показатели.

---

### Будущие улучшения ERP‑узла («на потом»)

- Перенос ORM‑репозиториев отчётов на SQL‑VIEW/материализованные VIEW на уровне БД (ErpVisitRevenueView, ErpVisitPayrollView, ErpVisitInventoryView, ErpLoyaltyObligationsView, ErpAttributionRevenueView) для разгрузки сложных ORM‑запросов и ускорения тяжёлых владельческих отчётов.
- Добавление исторических snapshot‑метрик по обязательствам (дневные/месячные срезы ErpLoyaltyObligationsView) поверх текущих состояний обязательств для более точной аналитики по динамике.
- Выделение отдельного доменного модуля для ERP‑отчетности (например, `erp_reports`) с консистентной схемой агрегаций по визитам, источникам трафика и кампаниям.