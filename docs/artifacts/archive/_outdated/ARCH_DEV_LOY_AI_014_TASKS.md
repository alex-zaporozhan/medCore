## ARCH_DEV_LOY_AI_014_TASKS — сверх‑детализированные dev‑таски

> Связанная архитектура: `ARCH_DEV_LOY_AI_014.md`  
> Связанный DEV_PROMPT: `DEV_PROMPT_LOY_AI_014` (P2, LOY‑3, TASK‑2, OMNI‑3)

---

### 1. Understand — инвентаризация текущих триггеров и кампаний

1.1. **Собрать все текущие механизмы Loyalty‑триггеров.**  
1.1.1. В сервисах Loyalty (истечения пакетов/депозитов, напоминания) найти все cron/Celery‑таски и ручные сценарии, которые сейчас реагируют на:  
       - истечение пакетов/балансов;  
       - низкую активность при высоком балансе;  
       - «спящих» клиентов.  
1.1.2. Зафиксировать используемые модели/таблицы, поля и правила отбора.

1.2. **Найти связи Loyalty ↔ Tasks/Attention.**  
1.2.1. По коду TaskService и AttentionFeed найти, какие события из Loyalty уже создают `AttentionItem`/`Task` (просроченные пакеты, долги и т.п.).  
1.2.2. Выписать типы задач/attention, которые уже используются для лояльности.

1.3. **Найти связи Loyalty ↔ Omnichannel/AI.**  
1.3.1. В Omnichannel‑слое и AI‑tools (registry/orchestrator) найти любые инструменты, которые используют данные Loyalty (балансы, пакеты).  
1.3.2. Зафиксировать, какие из них уже завязаны на кампании/уведомления, а какие только читают данные.

1.4. **Сопоставить с GAPS.**  
1.4.1. Открыть `BACKEND_GAPS_Loyalty_NEXT.md` (LOY‑3), `BACKEND_GAPS_Tasks_NEXT.md` (TASK‑2), `BACKEND_GAPS_Omnichannel_NEXT.md` (OMNI‑3).  
1.4.2. Для каждого gap указать, какие из найденных механизмов к нему относятся и где нет ещё AI‑уровня/кампаний.

---

### 2. Design‑to‑code — модель кампаний и настройки

2.1. **Спроектировать `LoyaltyCampaignSettings`.**  
2.1.1. На основе разделов 2–3 `ARCH_DEV_LOY_AI_014.md` определить структуру конфигурации per‑clinic: включённые кампании, лимиты (контакты/день, per‑patient), доступные каналы (Tasks, Omnichannel, SMS/e‑mail).  
2.1.2. Спроектировать ORM‑модель/таблицу и DTO для настроек (admin API), не смешивая их с низкоуровневыми Loyalty‑балансами.

2.2. **Спроектировать модель/интерфейс `LoyaltyCampaignEngine`.**  
2.2.1. В application‑слое определить интерфейс/класс `LoyaltyCampaignEngine` с методами:  
       - `run_campaigns_for_clinic(clinic_id, now, limit)`,  
       - (опционально) `simulate_campaign(campaign_id, clinic_id)` для аналитики.  
2.2.2. Разложить ответственность: сбор кандидатов (Loyalty‑сервисы), вызовы AI для персонализации (через tools‑registry), генерация Tasks/Omni‑черновиков.

2.3. **Спроектировать классы кампаний (MVP‑набор).**  
2.3.1. Формализовать минимум 2–3 типа кампаний из `ARCH_DEV_LOY_AI_014.md`:  
       - истекающие пакеты/депозиты;  
       - высокий баланс + низкая активность;  
       - реактивация «спящих» клиентов.  
2.3.2. Для каждой кампании описать правила отбора, каналы, ограничения по частоте и тип создаваемых Tasks/Attention.

2.4. **Спроектировать связь с AI Orchestrator.**  
2.4.1. На уровне дизайна определить, какие AI‑tools будут использоваться (через `ARCH_DEV_OMNI_REGISTRY_015.md`): генерация текста предложения, сегментация, подбор следующего действия.  
2.4.2. Зафиксировать требуемые входы/выходы AI‑tools и то, что наружу выходят только токенизированные идентификаторы (`PATIENT#...`, `LOYALTY_ACCOUNT#...`), в соответствии с `ARCH_DEV_AI_TOKENIZATION_025.md`.

---

### 3. Implement — движок кампаний и интеграция с AI

3.1. **Реализовать `LoyaltyCampaignSettings` и admin‑API.**  
3.1.1. Создать модель/таблицу и репозиторий для настроек кампаний, обеспечить фильтрацию по `clinic_id`.  
3.1.2. Добавить эндпоинты админки для просмотра/редактирования настроек (в соответствии с RBAC из `ARCH_DEV_SEC_RBAC_022.md`).

3.2. **Реализовать `LoyaltyCampaignEngine`.**  
3.2.1. Реализовать метод `run_campaigns_for_clinic`:  
       - чтение настроек кампаний;  
       - отбор кандидатов по правилам (SQL/сервисы Loyalty);  
       - применение лимитов per‑clinic/per‑patient.  
3.2.2. Для MVP реализовать бизнес‑логику отбора без AI, используя только доменные данные Loyalty.

3.3. **Интегрировать AI‑слой (опционально для MVP).**  
3.3.1. Если по приоритету включается AI‑персонализация:  
       - реализовать вызовы AI‑tools через Omnichannel Orchestrator для генерации текста/сегментации;  
       - убедиться, что все вызовы идут через `SafeAiClient` + `AiSanitizer` + tokenization‑слой.  
3.3.2. Обработать ошибки AI: fallback на дефолтные шаблоны без срыва кампании.

3.4. **Создать Tasks/Attention и Omnichannel‑черновики.**  
3.4.1. Для каждого срабатывания кампании создавать `AttentionItem`/`Task` с типами `LOYALTY_EXPIRING_PACKAGE`, `LOYALTY_HIGH_BALANCE_LOW_ACTIVITY`, `LOYALTY_REENGAGEMENT` (по `ARCH_DEV_TASKS_MODEL_020.md`).  
3.4.2. При наличии Omnichannel — создавать черновики диалогов/сообщений, которые оператор сможет отредактировать и отправить.

---

### 4. Integrate — связи с Tasks, Omnichannel и UI

4.1. **Интеграция с Tasks/Attention.**  
4.1.1. Обновить TaskService/AttentionFeed так, чтобы новые типы лояльностных задач корректно отображались в CRM/админке (фильтры, списки, приоритеты).  
4.1.2. Убедиться, что есть ссылки на пациента, клинику, тип кампании и источник (`created_by="ai"`/`system"`).

4.2. **Интеграция с Omnichannel.**  
4.2.1. В Omnichannel UI добавить отображение лояльностных кампаний/рекомендаций как один из источников действий (по `ARCH_OMNICHANNEL_NEXT.md`).  
4.2.2. Обеспечить, чтобы оператор видел: контекст кампании, предложенный текст, лимиты и мог принять/отклонить/отредактировать действие.

4.3. **Интеграция с Loyalty/CRM UI.**  
4.3.1. На страницах клиента/лояльности/CRM отобразить базовую информацию о сработавших кампаниях и связанных задачах (истекающие пакеты, высокие балансы и т.п.).  
4.3.2. Убедиться, что UI не обходит RBAC (просмотр только в пределах доступных клиник/ролей).

---

### 5. Observe — логи, метрики и контроль шума

5.1. **Логирование кампаний и действий.**  
5.1.1. В `LoyaltyCampaignEngine` логировать: запуски кампаний, количество отобранных кандидатов, количество созданных Tasks/Omni‑черновиков, `clinic_id`, тип кампании.  
5.1.2. Логи не должны содержать ПД (ФИО, телефоны), только ID/токены и агрегаты.

5.2. **Метрики эффективности и шума.**  
5.2.1. Ввести метрики:  
       - число срабатываний кампаний и созданных задач/контактов;  
       - отклик (доля задач/черновиков, приведших к записи/продлению);  
       - уровень отказов/жалоб (если есть канал обратной связи).  
5.2.2. Использовать эти метрики для настройки лимитов и приоритизации кампаний.

5.3. **Контроль частоты и этики контактов.**  
5.3.1. Реализовать технический контроль лимитов (не более N кампаний/месяц на пациента, исключение opt‑out категорий) согласно разделу 5.3 `ARCH_DEV_LOY_AI_014.md`.  
5.3.2. Добавить в настройки возможность временно отключать отдельные кампании/каналы без удаления конфигурации.

---

### 6. Stabilize — тесты, SEC/RBAC и синхронизация артефактов

6.1. **Покрыть движок кампаний тестами.**  
6.1.1. Добавить юнит‑/интеграционные тесты на отбор кандидатов для каждого класса кампаний (истекающие пакеты, высокий баланс, реактивация).  
6.1.2. Проверить соблюдение лимитов per‑clinic/per‑patient и отсутствие дублирующих задач по одному пациенту в коротком интервале.

6.2. **Проверить RBAC/SEC и PD‑ограничения.**  
6.2.1. Убедиться, что управление кампаниями и просмотр их результатов защищены пермишенами (`loyalty.campaigns.manage`, `loyalty.campaigns.run`, опционально `ai.loyalty.analyze`) по `ARCH_DEV_SEC_RBAC_022.md`.  
6.2.2. Проверить, что при использовании AI все вызовы проходят через `SafeAiClient`/`AiSanitizer`/tokenization и не передают внешним провайдерам реальные ПД.

6.3. **Синхронизировать DEV_PROMPT и GAPS.**  
6.3.1. Обновить `DEV_PROMPTS_NEXT.md`, указав статус `DEV_PROMPT_LOY_AI_014` и краткое резюме реализованных кампаний.  
6.3.2. В `BACKEND_GAPS_Loyalty_NEXT.md`, `BACKEND_GAPS_Tasks_NEXT.md`, `BACKEND_GAPS_Omnichannel_NEXT.md` и при необходимости `NONFUNCTIONAL_AUDIT_NEXT.md` отметить закрытые/уточнённые пункты по LOY‑3/TASK‑2/OMNI‑3.

## ARCH_DEV_LOY_AI_014_TASKS — сверх‑детализированные dev‑таски

> Связанная архитектура: `ARCH_DEV_LOY_AI_014.md`  
> Связанный DEV_PROMPT: `DEV_PROMPT_LOY_AI_014` (P3, LOY‑3, TASK‑2, OMNI‑3)

---

### 1. Understand — текущие механизмы уведомлений и кампаний Loyalty

1.1. **Инвентаризация текущих Loyalty‑уведомлений и «кампаний».**  
1.1.1. В сервисах Loyalty (и Celery‑тасках, если есть) найти:  
       - любые механизмы истечения пакетов/депозитов (cron/periodic tasks);  
       - уведомления о низком балансе/отсутствии активности;  
       - существующие промо/маркетинговые рассылки.  
1.1.2. Зафиксировать, какие из них:  
       - уже оформлены как Tasks/Attention;  
       - уходят напрямую во внешние каналы (SMS/e‑mail/Omni) без единой модели кампаний.

1.2. **Инвентаризировать источники данных для будущих AI‑кампаний.**  
1.2.1. По `ARCH_LOYALTY_NEXT.md` и коду собрать:  
       - где и как считаются балансы пакетов/депозитов;  
       - где хранится информация об истечении (`expires_at`), активности (последний визит/списание), структуре FamilyLink (через `ARCH_DEV_LOY_FAMILY_013`).  
1.2.2. Определить, какие агрегаты/метрики уже есть, а какие потребуют дополнительных запросов/матриц.

1.3. **Инвентаризация Tasks/Attention, связанных с лояльностью.**  
1.3.1. В `ARCH_TASKS_NEXT.md`, `ARCH_DEV_TASKS_MODEL_020.md`, коде Tasks/Attention‑сервисов найти:  
       - типы задач/Attention, уже используемые для Loyalty (если есть);  
       - связи с пациентами/пакетами/депозитами.  
1.3.2. Сопоставить с `BACKEND_GAPS_Tasks_NEXT.md` (TASK‑2) и `BACKEND_GAPS_Loyalty_NEXT.md` (LOY‑3), какие сигналы нужно ещё формализовать.

1.4. **Сопоставление с Omnichannel/AI‑инфраструктурой.**  
1.4.1. По `ARCH_OMNICHANNEL_NEXT.md`, `ARCH_DEV_OMNI_REGISTRY_015.md`, `ARCH_DEV_OMNI_POLICY_016.md`, `ARCH_DEV_AI_TOKENIZATION_025.md` определить:  
       - какие уже есть AI‑tools/цепочки, которые могут использовать Loyalty (анализ истории, сегментация клиентов);  
       - как сейчас обеспечивается политика ПД/tokenization.  
1.4.2. Зафиксировать ограничения на кампании (частота контактов, каналы, требования к анонимизации).

---

### 2. Design‑to‑code — AI‑кампании лояльности, настройки и компоненты

2.1. **Уточнить MVP‑набор AI‑кампаний.**  
2.1.1. На основе `ARCH_DEV_LOY_AI_014.md` и бизнес‑приоритетов зафиксировать 2–3 сценария v1:  
       - истекающие пакеты/депозиты;  
       - низкая активность при высоком балансе;  
       - «спящие» клиенты с историей лояльности.  
2.1.2. Для каждого сценария описать:  
       - критерии отбора (временные окна, пороги балансов/активности);  
       - целевое действие (Task/Attention/Omni‑черновик).

2.2. **Спроектировать `LoyaltyCampaignSettings`.**  
2.2.1. Оформить модель/таблицу `LoyaltyCampaignSettings` per‑clinic с полями:  
       - `clinic_id`;  
       - список включённых кампаний/флагов (`expiring_packages`, `high_balance_low_activity`, `reengagement`);  
       - лимиты: максимум контактов/день на клинику/пациента;  
       - допустимые каналы (Tasks/Omni/SMS/email и т.п., если релевантно).  
2.2.2. Определить интерфейс управления настройками для админки (backend‑API + фронт‑форма в отдельных задачах).

2.3. **Спроектировать компоненты `LoyaltyCampaignEngine` и `LoyaltyCampaignAnalyzer`.**  
2.3.1. На уровне application‑слоя описать:  
       - `LoyaltyCampaignEngine`:  
         - метод `run_for_clinic(clinic_id, settings, date_range/now)` → список кандидатных действий;  
       - `LoyaltyCampaignAnalyzer` (AI‑часть, может быть AI‑tool):  
         - методы для сегментации клиентов, генерации текстов/рекомендаций.  
2.3.2. Зафиксировать, что Engine использует доменные сервисы/агрегаты Loyalty, а Analyzer — AI‑инфраструктуру через tools‑registry и соблюдает политику ПД.

2.4. **Спроектировать формат campaign‑кандидатов и Tasks/Attention.**  
2.4.1. Описать DTO `LoyaltyCampaignCandidate`:  
       - `clinic_id`, `patient_id` (или токен для AI);  
       - `campaign_type`;  
       - ключевые параметры (остаток, срок, активность);  
       - рекомендуемое действие/сообщение (структура или ссылка на AI‑generator).  
2.4.2. Определить маппинг кандидатов в Tasks/Attention и/или Omni‑черновики: типы задач (`LOYALTY_EXPIRING_PACKAGE`, `LOYALTY_HIGH_BALANCE_LOW_ACTIVITY`, `LOYALTY_REENGAGEMENT`) и их поля.

---

### 3. Implement — сбор кандидатов и запуск кампаний

3.1. **Реализовать сбор данных для кампаний.**  
3.1.1. В `LoyaltyCampaignEngine` реализовать логику выборки кандидатов для каждого сценария:  
       - истекающие пакеты: выборка Form/балансов/пакетов с `expires_at` в нужном интервале и ненулевым остатком;  
       - высокий баланс/низкая активность: клиенты с большим остатком и без визитов/списаний за период;  
       - реэнгейджмент: клиенты без визитов длительное время, но с историей лояльности.  
3.1.2. Использовать только публичные сервисы/агрегаты Loyalty/Booking/ERP, не лезть в «сырые» SQL руками, если есть доменные обёртки.

3.2. **Реализовать преобразование кандидатов в Tasks/Attention.**  
3.2.1. Для каждого найденного `LoyaltyCampaignCandidate` реализовать маппинг в:  
       - `AttentionItem` (описание риска/возможности), и/или  
       - `Task` для оператора (суть предложения, срок, приоритет, `created_by="ai"` или `system`).  
3.2.2. Использовать TaskService/AttentionFeedService в соответствии с `ARCH_DEV_TASKS_MODEL_020.md` и `ARCH_DEV_TASKS_AI_021.md`.

3.3. **Интегрировать AI‑уровень (Analyzer) при необходимости.**  
3.3.1. Если в v1 планируется подключить LLM для персонализации сообщений/рекомендаций:  
       - зарегистрировать AI‑tool (например, `analyze_loyalty_for_campaigns`) в tools‑registry;  
       - реализовать обёртку `LoyaltyCampaignAnalyzer`, которая:  
         - принимает список кандидатов (с токенами и агрегатами, без ПД);  
         - возвращает тексты/рекомендации или дополнительные маркеры для приоритезации.  
3.3.2. При недоступности AI‑уровня обеспечить graceful‑fallback (кампании работают на простых правилах).

3.4. **Учесть лимиты и избежание «спама».**  
3.4.1. В `LoyaltyCampaignEngine` реализовать применение `LoyaltyCampaignSettings`:  
       - фильтровать кандидатов по лимитам на клинику/пациента;  
       - не создавать повторные задачи/Attention слишком часто;  
       - учитывать отказ/отписку пациентов (если такая информация хранится).  
3.4.2. Задокументировать бизнес‑правила частоты и каналов (минимальный набор для v1).

---

### 4. Integrate — связь с Omnichannel и UI

4.1. **Интеграция с Tasks/Attention‑UI.**  
4.1.1. Убедиться, что создаваемые Tasks/Attention по лояльности:  
       - отображаются в админских UI списков задач и Attention;  
       - имеют понятные типы, лейблы и фильтры (например, фильтр по «Лояльность/кампании»).  
4.1.2. При необходимости скорректировать фронтовые модели/словари типов Tasks/Attention в отдельных задачах.

4.2. **Интеграция с Omnichannel‑кампаниями.**  
4.2.1. При наличии Omnichannel/AI:  
       - добавить в Orchestrator сценарии, которые по `LoyaltyCampaignCandidate`/Task могут генерировать:  
         - черновики сообщений;  
         - предложения действий для оператора в диалоге.  
4.2.2. Связать эти сценарии с Booking/CRM‑контекстом (пациент, FamilyLink, ближайший визит/рекомендация).

4.3. **Уважение FamilyLink и семейного контекста.**  
4.3.1. При формировании кандидатов и задач учитывать модель FamilyLink (`ARCH_DEV_LOY_FAMILY_013`):  
       - не предлагать кампании, нарушающие права/политику семьи;  
       - при необходимости создавать задачи на владельца ресурса, а не на бенефициара.  
4.3.2. Проверить, что UI/Omni‑сценарии не раскрывают лишнюю информацию о других участниках семьи.

---

### 5. Observe — логи, метрики и SEC/RBAC для AI‑кампаний

5.1. **Логирование запуска и работы кампаний.**  
5.1.1. В `LoyaltyCampaignEngine` и связанных задачах логировать:  
       - время и параметры запуска (клиника, тип кампаний, объём выборки);  
       - количество найденных кандидатов и созданных Tasks/Attention/Omni‑черновиков;  
       - ошибки при выполнении (с кодами/контекстом).  
5.1.2. Логи не должны содержать ПД (только идентификаторы и агрегаты).

5.2. **Метрики по эффективности кампаний.**  
5.2.1. Ввести метрики:  
       - число кампаний и вовлечённых пациентов по периодам/клиникам/типам;  
       - базовые показатели конверсии (по возможности):  
         - доля задач/кандидатов, приведших к визиту/продлению;  
         - доля отказов/жалоб (если есть обратная связь).  
5.2.2. Интегрировать эти метрики с OBS‑цепочками (`ARCH_DEV_OBS_CHAINS_023.md`) и ERP/Attribution‑отчётами для оценки ROI кампаний.

5.3. **SEC/RBAC и контроль шума/этики.**  
5.3.1. Убедиться, что операции запуска/управления кампаниями:  
       - защищены пермишенами (`loyalty.campaigns.manage`, `loyalty.campaigns.run`, `ai.loyalty.analyze` и т.п. из `ARCH_DEV_SEC_RBAC_022.md`);  
       - не доступны ролям без полномочий.  
5.3.2. Настроить/задокументировать политику шум/этика:  
       - лимиты частоты контактов;  
       - фильтры по отписавшимся/чувствительным категориям;  
       - предпочтительные каналы и условия их использования.

---

### 6. Stabilize — тесты, документация и связь с GAPS

6.1. **Покрыть Loyalty‑кампании тестами.**  
6.1.1. Написать/обновить unit‑тесты для `LoyaltyCampaignEngine`:  
       - корректный выбор кандидатов для каждого сценария на синтетических данных;  
       - соблюдение лимитов и настроек;  
       - отсутствие дубликатов и повторного спама по одному пациенту.  
6.1.2. Добавить тесты для интеграции с Tasks/Attention:  
       - корректное создание задач/Attention нужных типов;  
       - правильные поля (`created_by="ai"`/`system`, ссылки на пациента/ресурс).

6.2. **Интеграционные тесты с Omnichannel/Tasks/AI.**  
6.2.1. При наличии Omnichannel/AI‑интеграций добавить сценарии (или описать для QA):  
       - запуск кампании → создание задач → оператор в Omnichannel/CRM обрабатывает их;  
       - сценарии с AI‑персонализацией сообщений (если реализовано).  
6.2.2. Убедиться, что при ошибках AI‑уровня (недоступность LLM, ошибки Orchestrator’а) доменные данные Loyalty/Tasks не остаются в неконсистентном состоянии.

6.3. **Синхронизация документации и GAPS.**  
6.3.1. После реализации обновить:  
       - `DEV_PROMPTS_NEXT.md` — статус `DEV_PROMPT_LOY_AI_014`;  
       - `BACKEND_GAPS_Loyalty_NEXT.md` (LOY‑3), `BACKEND_GAPS_Tasks_NEXT.md` (TASK‑2), `BACKEND_GAPS_Omnichannel_NEXT.md` (OMNI‑3) — отметить закрытые/уточнённые пункты;  
       - при необходимости `NONFUNCTIONAL_AUDIT_NEXT.md` — указать изменения по OBS/PERF для кампаний.  
6.3.2. Зафиксировать в `ARCH_AUDIT_NEXT.md` ключевые решения по AI‑кампаниям лояльности и их ограничения (этика, PD, каналы).

---

## Статус реализации (@QA_ARCH / 2026‑03)

**Закрыто по MVP (правила + Tasks + админка):**

| Блок | Состояние |
|------|-----------|
| Модель `LoyaltyCampaignSettings`, API, RBAC | ✅ |
| `LoyaltyCampaignEngine` / `run_campaigns_for_clinic`, типы `LOYALTY_*` | ✅ |
| Лимиты день/месяц, cooldown, пересечение кампаний, SMS‑дедуп с expiring | ✅ |
| Opt‑out пациента (уведомления / напоминания / маркетинг) | ✅ |
| Логи/метрики, Celery beat, индекс БД | ✅ |
| UI: вкладка «Кампании» в лояльности админки | ✅ |
| AI‑слой, Omni‑черновики, FamilyLink‑владелец | ❌ не в MVP |
| Полная синхронизация GAPS/DEV_PROMPTS/ARCH_AUDIT | ⚠️ частично (см. `BACKEND_GAPS_Loyalty_NEXT`) |

Подробный перечень — в **`ARCH_DEV_LOY_AI_014.md` §8**.

### На потом (предложения по улучшению)

1. **AI / Omnichannel:** зарегистрировать tool (например `analyze_loyalty_for_campaigns`), вызывать через Orchestrator + `SafeAiClient` + токены; черновики сообщений при `channel_omnichannel_enabled`.
2. **FamilyLink:** создавать/маршрутизировать задачи на `primary_patient_id`, если лояльность «семейная»; не дублировать касания бенефициару/владельцу (согласовать с `ARCH_DEV_LOY_FAMILY_013`).
3. **Продукт / согласия:** явная политика «операционное vs маркетинг» (сейчас реактивация завязана на `consent_mailing`); опционально отдельный флаг «кампании лояльности» у пациента.
4. **Наблюдаемость:** связать `loyalty_campaign_*` с OBS‑цепочками; KPI конверсии (задача → визит/продление) в отчётах.
5. **Тесты:** стабильный CI с тестовой БД; сценарии opt‑out и месячного лимита; нагрузочный прогон COUNT по `tasks`.
6. **Документы:** обновить `DEV_PROMPTS_NEXT.md` статус промпта, `ARCH_AUDIT_NEXT.md` решения; при необходимости `BACKEND_GAPS_Tasks_NEXT` / `BACKEND_GAPS_Omnichannel_NEXT` для TASK‑2/OMNI‑3.
7. **Docker/ops:** проверить наличие `frontend/.env` для compose; секреты только через `.env`.
8. **Tasks UI / CRM:** явные фильтры и подписи в админских списках задач для `attention_kind` вида `LOYALTY_*` (и в Attention feed при необходимости), чтобы оператор отличал кампании от прочих задач.
9. **Границы «дня» и часового пояса:** сейчас лимиты «за день» считаются от UTC; для продукта может понадобиться привязка к календарю клиники или таймзоне из настроек (`Europe/Moscow` и т.д.).
10. **Единый бюджет контактов:** общий счётчик «касаний» на пациента в день между SMS (`check_expiring_packages`), push и LOYALTY‑задачами — чтобы не пересекались два независимых контура.
11. **Ручной запуск API:** rate limiting или idempotency key на `POST /campaigns/run`, аудит «кто запустил» (`admin_id` в логе/событии), защита от случайных двойных кликов.
12. **Версионирование настроек:** история изменений `LoyaltyCampaignSettings` (кто/когда) для разборов инцидентов и соответствия политике.
13. **Устойчивость Celery:** DLQ / retry policy для `run_loyalty_campaign_engine_all_clinics` при частичных сбоях БД; алерт по `loyalty_campaign_batch_errors_total`.
14. **SQLite / dev:** при необходимости локальных прогонов без PostgreSQL — fallback для `INSERT … ON CONFLICT` в настройках кампаний (сейчас ориентир на PG).
15. **Шаблоны текстов:** вынести дефолтные русские строки задач в конфиг/локализацию или шаблоны клиники (без изменения доменной логики отбора).

---

