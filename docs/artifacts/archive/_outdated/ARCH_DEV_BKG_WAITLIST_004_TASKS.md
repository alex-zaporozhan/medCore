## ARCH_DEV_BKG_WAITLIST_004_TASKS — сверх‑детализированные dev‑таски

> Связанная архитектура: `ARCH_DEV_BKG_WAITLIST_004.md`  
> Связанный DEV_PROMPT: `DEV_PROMPT_BKG_WAITLIST_004` (P2, BKG‑4, FADM‑2)

---

### 1. Understand — инвентаризация текущего листа ожидания

1.1. **Найти все модели/таблицы, связанные с waitlist.**  
1.1.1. В доменном/ORM‑слое (например, `src/domain/entities/*`, `src/infrastructure/db/*`) найти сущности/таблицы, в названиях которых фигурируют `waitlist`, `queue`, `waiting`, `reservation` и т.п.  
1.1.2. Для каждой найденной сущности выписать:  
       - поля, относящиеся к клинике/пациенту/услуге/времени/врачу;  
       - статусы/флаги (если есть);  
       - связи с `Booking`/расписанием.

1.2. **Инвентаризировать backend‑сервисы и роутеры waitlist.**  
1.2.1. В `src/api/v1/routers/admin_waitlist.py` и смежных файлах найти все эндпоинты, работающие с листом ожидания:  
       - создание/редактирование/удаление заявок;  
       - получение списков;  
       - действия по переводу в `Booking`.  
1.2.2. В сервисах расписания/записей (`ScheduleService`, `BookingService` и др.) найти любые методы, которые:  
       - обращаются к waitlist;  
       - используют waitlist при отменах/освободившихся слотах.

1.3. **Разобрать текущие статусы/поля и UX‑потоки.**  
1.3.1. По коду роутеров/сервисов и фронта (админка/PWA) зафиксировать:  
       - какие статусы/флаги waitlist уже используются (строки/enum);  
       - какие действия доступны администраторам и пациентам (создать, отписаться, принять слот, проигнорировать и т.п.).  
1.3.2. Сопоставить эти потоки с UX‑описанием в `UX_FLOWS_AND_GAPS_NEXT.md` (BKG‑4, FADM‑2), выписав расхождения.

1.4. **Сопоставить с GAPS и соседними ARCH_DEV.**  
1.4.1. Открыть `BACKEND_GAPS_Booking_NEXT.md` (BKG‑4) и `UX_FLOWS_AND_GAPS_NEXT.md` (FADM‑2); отметить:  
       - где waitlist реализован частично/legacy;  
       - какие проблемы (потеря пациентов, непредсказуемые правила) нужно закрыть.  
1.4.2. Согласовать этот список с `ARCH_DEV_BKG_CORE_001.md`, `ARCH_DEV_BKG_STATE_002.md`, `ARCH_DEV_BKG_MULTI_003.md`, чтобы не противоречить фасаду завершения визита, единому enum статусов и multi‑clinic правилам.

---

### 2. Design‑to‑code — целевая модель `WaitlistEntry` и статусы

2.1. **Спроектировать финальную модель `WaitlistEntry`.**  
2.1.1. На основе `ARCH_DEV_BKG_WAITLIST_004.md` оформить доменную/ORM‑модель `WaitlistEntry` с полями:  
       - идентификация: `id`, `clinic_id`, `patient_id`;  
       - предпочтения: `preferred_doctor_id`, `preferred_service_id`, временное окно (`preferred_time_from`/`_to`), приоритет;  
       - контекст: `source` (admin/pwa/omni/crm), примечания;  
       - связи: `booking_id` (если создана запись);  
       - статус: `status` (см. 2.2);  
       - служебные поля: `created_at`, `updated_at`, `created_by`, `updated_by`.  
2.1.2. Определить индексы по частым фильтрам (clinic, doctor, service, статус).

2.2. **Зафиксировать список статусов и допустимых переходов.**  
2.2.1. На уровне домена определить enum статусов `WaitlistStatus` (минимум):  
       - `waiting`, `notified`, `booked`, `cancelled`, `expired`.  
2.2.2. Описать матрицу переходов `from → to` и триггеров (события расписания, действия админа/пациента), согласовав её с `ARCH_BOOKING_NEXT.md` и UX‑флоу (какие статусы когда видны в UI).

2.3. **Спроектировать `WaitlistService` как единый фасад waitlist‑логики.**  
2.3.1. В application‑слое описать интерфейс/класс `WaitlistService` с методами:  
       - `create_entry(...) -> WaitlistEntry`;  
       - `update_entry(...)`;  
       - `cancel_entry(...)`;  
       - `mark_notified(...)`;  
       - `convert_to_booking(entry_id, slot/booking_data, ...) -> Booking`;  
       - методы поиска подходящих записей по событиям расписания.  
2.3.2. Зафиксировать, что все операции с waitlist должны использовать этот сервис, а не обходить его.

2.4. **Продумать стратегию миграции legacy‑структур в `WaitlistEntry`.**  
2.4.1. На основе анализа из раздела 1 определить:  
       - какие старые таблицы/поля будут:  
         - перенесены в новую модель;  
         - оставлены как архив/temporary;  
       - как будут инициализированы новые поля (например, статус `waiting` по умолчанию).  
2.4.2. Задокументировать в коде/миграциях особенности переноса (ограничения по истории, невозможность восстановить часть информации и т.п.).

---

### 3. Implement — backend: сервисы, триггеры и API

3.1. **Реализовать `WaitlistService` с единым жизненным циклом.**  
3.1.1. Создать/обновить сервис `WaitlistService`, реализующий методы из 2.3:  
       - с проверками допустимости статусов и переходов;  
       - с логированием ключевых действий.  
3.1.2. Обеспечить, чтобы сервис:  
       - уважал `clinic_id` (мультиклиника по `ARCH_DEV_BKG_MULTI_003.md`);  
       - не допускал создания записей для чужой клиники/пациента.

3.2. **Обновить роутеры admin/PWA для работы через `WaitlistService`.**  
3.2.1. В `admin_waitlist.py` и PWA‑роутерах:  
       - заменить прямую работу с БД/legacy‑сервисами на вызовы `WaitlistService`;  
       - привести контракт API к новой модели (вход/выход по `WaitlistEntry` + фильтры).  
3.2.2. Убедиться, что все операции создания/редактирования/удаления/перевода waitlist‑записей используют единый сервис.

3.3. **Реализовать триггеры по расписанию и отменам.**  
3.3.1. В `ScheduleService`/Celery‑тасках реализовать реакцию на события:  
       - отмена `Booking`/освобождение слота;  
       - появление «дырок» в расписании.  
3.3.2. Для каждого такого события вызывать `WaitlistService` для поиска подходящих `WaitlistEntry` и:  
       - инициировать уведомление пациента;  
       - или создавать черновую/подтверждаемую `Booking` (по бизнес‑политике).  
3.3.3. Обеспечить защиту от гонок/дубликатов (например, транзакциями или блокировками на уровне записей waitlist).

3.4. **Реализовать перевод waitlist → Booking.**  
3.4.1. В `WaitlistService.convert_to_booking` реализовать:  
       - проверку статуса `WaitlistEntry` (только допустимые состояния, например `waiting`/`notified`);  
       - вызов `BookingService` для создания/обновления записи;  
       - проставление `booking_id` в waitlist и перевод статуса в `booked` (или другой согласованный).  
3.4.2. Обработать ошибки:  
       - конфликт слота, отказ в правах, неконсистентное состояние (создавать контролируемый результат + лог/Attention).

---

### 4. Integrate — связь с Omnichannel, CRM и фасадом Booking

4.1. **Интеграция с Omnichannel/CRM для создания/управления waitlist.**  
4.1.1. В Omnichannel/CRM‑сервисах и UI (по описанию в `ARCH_OMNICHANNEL_NEXT.md`/`ARCH_CRM_NEXT.md`):  
       - обеспечить возможность создания `WaitlistEntry` из диалога/лида;  
       - использовать API/сервисы `WaitlistService`, а не прямые записи в БД.  
4.1.2. Согласовать, как AI/операторы видят и управляют waitlist в этих модулях (без дублирования логики).

4.2. **Интеграция с фасадом завершения визита и статусами Booking.**  
4.2.1. Убедиться, что использование waitlist не конфликтует с фасадом `BookingCompletionService` (`ARCH_DEV_BKG_CORE_001.md`):  
       - waitlist управляет только до создания записи/изменения слота, а не вмешивается в завершение визита.  
4.2.2. Проверить, что статусы Booking (`ARCH_DEV_BKG_STATE_002.md`) и стейт‑машина не позволяют «странных» переходов из waitlist‑генерируемых записей.

4.3. **Связь с Tasks/Attention.**  
4.3.1. Определить события, которые должны порождать `AttentionItem`/Task (см. 5.2) и реализовать их через `TaskService`/`AttentionFeedService`:  
       - долгождущие high‑priority пациенты;  
       - системные сбои триггеров;  
       - аномально большое количество `waiting` по врачу/услуге/клинике.

---

### 5. Observe — логи, метрики и сигналы по waitlist

5.1. **Добавить структурированное логирование для waitlist‑операций.**  
5.1.1. В `WaitlistService` и связанных роутерах логировать:  
       - события создания/обновления/закрытия `WaitlistEntry`;  
       - попытки автоматического подбора слота и их исход;  
       - параметры: `clinic_id`, `patient_id` (ID), предпочтения (услуга/врач/время), `status`, инициатор.  
5.1.2. Убедиться, что логи не содержат ПД (ФИО/телефон), только идентификаторы и агрегаты.

5.2. **Метрики качества работы waitlist.**  
5.2.1. Ввести метрики (интегрируемые с `ARCH_DEV_OBS_CHAINS_023.md`):  
       - количество активных `waiting`/`notified` по клинике/врачу/услуге;  
       - среднее/медианное время от создания до `booked`/`cancelled`/`expired`;  
       - конверсия waitlist → Booking;  
       - частота автоматических/ручных переводов.  
5.2.2. При необходимости добавить алерты по некоторым показателям (например, слишком большое среднее время ожидания для high‑priority записей).

5.3. **Tasks/Attention для аномалий и сбоев.**  
5.3.1. Для критичных случаев (длительное ожидание, системные ошибки триггеров, постоянные конфликты при конвертации в Booking) реализовать создание `AttentionItem`/Tasks по шаблону `ARCH_DEV_TASKS_MODEL_020.md`:  
       - типы, например: `WAITLIST_LONG_WAIT`, `WAITLIST_TRIGGER_FAILURE`, `WAITLIST_BOOKING_CONFLICT`.  
5.3.2. Привязывать эти сигналы к `clinic_id`, врачу/услуге, и, при необходимости, к конкретным пациентам (через ID/токены).

---

### 6. Stabilize — тесты, миграции и обновление артефактов

6.1. **Миграции и обратная совместимость.**  
6.1.1. Подготовить миграции для новой/обновлённой модели `WaitlistEntry`:  
       - перенос существующих данных из legacy‑таблиц (если есть);  
       - осторожная инициализация статусов и предпочтений по дефолтам.  
6.1.2. Задокументировать, какие аспекты истории могут быть утеряны/обобщены при миграции и почему это приемлемо.

6.2. **Покрытие waitlist‑логики тестами.**  
6.2.1. Написать/обновить unit‑тесты для `WaitlistService`:  
       - создание/отмена/изменение записей с разными предпочтениями;  
       - корректные и некорректные переходы статусов;  
       - конвертация в Booking (успех/ошибка).  
6.2.2. Добавить интеграционные тесты для цепочек:  
       - отмена `Booking` → поиск в waitlist → создание новой записи/уведомление;  
       - PWA/Omnichannel‑создание waitlist → реакция при появлении слота.

6.3. **Синхронизация документации и GAPS.**  
6.3.1. После завершения реализации обновить:  
       - `DEV_PROMPTS_NEXT.md` — статус `DEV_PROMPT_BKG_WAITLIST_004`;  
       - `BACKEND_GAPS_Booking_NEXT.md` (BKG‑4) и `UX_FLOWS_AND_GAPS_NEXT.md` (FADM‑2) — отметить закрытые/уточнённые пункты;  
       - при необходимости `ARCH_AUDIT_NEXT.md` — описать новый waitlist‑слой и наблюдаемость по нему.  
6.3.2. Убедиться, что waitlist‑реализация согласована с другими Booking‑промптами (STATE/MULTI/ERRORS) и не ломает их инварианты.

---

## Выполнено (v1 core, @DEV / @QA_ARCH, 2026‑03)

Краткий фактический срез; развёрнуто в **`ARCH_DEV_BKG_WAITLIST_004.md` §8**.

- [x] **§2–3 (ядро):** `WaitlistStatus`, расширенная `WaitlistEntry`, миграция `i5j6k7l8m9n0`, `WaitlistService` (жизненный цикл, мультиклиника, блокировка конвертации, компенсация при сбое `mark_booked`).
- [x] **§3.2–3.4:** `admin_waitlist` через сервис; конвертация waitlist → booking в `BookingService` + `mark_booked_after_booking_created`; инвалидация кеша по фактическому врачу/дате.
- [x] **§3.3 (часть):** триггер при **отмене** booking → `notify_slot_freed` (фильтр по услуге, sequential/broadcast, `max_notifications_per_entry`, блокировки).
- [x] **§5 (часть):** структурированные логи (без ПД в явном виде), Prometheus‑счётчики waitlist.
- [x] **§6 (часть):** миграция; unit‑тесты домена; интеграционные тесты сервиса (при доступной тестовой БД); idempotent DDL в `tests/conftest` для колонок waitlist.
- [x] **QA‑фиксы (итерация @QA_ARCH):** запрет правок терминальных записей; `include_booked` в API/фронте; исправление гонки и несогласованности booking↔waitlist.

---

## На потом (@QA_ARCH)

Улучшения и хвосты **после** v1 core; не блокируют эксплуатацию единого сервиса и admin‑потока.

| # | Область | Предложение |
|---|----------|-------------|
| 1 | **Интеграция §4.1** | Публичные/PWA‑эндпоинты waitlist для пациента; создание заявки из Omnichannel/CRM через `WaitlistService` без прямых INSERT. |
| 2 | **Триггеры §3.3** | Реакция на «дыры» в расписании **без** отмены booking (новые слоты, пересборка дня) — отдельные события/Celery. |
| 3 | **Expire** | Периодическая задача: перевод в `expired` по политике/возрасту заявки; уведомление пациента. |
| 4 | **§5.2–5.3 Observe** | Гистограммы времени (создание → booked/cancelled), дашборд конверсии; Attention/Tasks: `WAITLIST_LONG_WAIT`, `WAITLIST_TRIGGER_FAILURE`, `WAITLIST_BOOKING_CONFLICT` при сбоях компенсации или длительном `waiting`. |
| 5 | **Тесты §6.2** | E2E в CI: cancel booking → waitlist; полное покрытие конвертации с БД в pipeline. |
| 6 | **Документация §6.3** | Точечные правки `UX_FLOWS_AND_GAPS_NEXT.md` (FADM‑2: связка schedule↔waitlist), `ARCH_AUDIT_NEXT` при появлении цепочек наблюдаемости. |
| 7 | **Продукт** | Переключатель в UI «только активная очередь / с завершёнными» поверх `include_booked` (сейчас параметр задаётся в хуке). |

---
