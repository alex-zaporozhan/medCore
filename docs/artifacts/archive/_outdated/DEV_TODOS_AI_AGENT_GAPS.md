## DEV_TODOS_AI_AGENT_GAPS — хвосты по AI‑агенту (Function Calling)

> Основано на `DEV_PROMPTS_AI_AGENT.md` и текущем коде (`omnichannel_ai_orchestrator`, `tools_*`, `AiConfigService`, security‑тесты).

---

## 1. Orchestrator loop и интеграция с Omnichannel

- [ ] **1.1. Завершённость цикла tool‑вызовов**
  - Проверить и при необходимости доработать `OmnichannelAIOrchestrator.run_ai_agent`:
    - что все ветки (нет tools, ошибка tools, ошибка LLM) возвращают осмысленный `AgentResult`;
    - что ограничение по `max_tool_iterations` и общему времени (`max_total_duration_seconds`) действительно применяется и покрыто тестами.

- [ ] **1.2. Интеграция с Omnichannel‑чатом**
  - Убедиться, что:
    - AI‑ответы записываются как `OmnichannelMessage` с корректным `actor_type`/метаданными;
    - при ошибках/таймаутах создаётся человеко‑ориентированное fallback‑сообщение («администратор подключится лично»), как описано в промпте.
  - Если какая‑то из веток сейчас `return None` без записи сообщения в чат — вынести это в явный TODO и согласованный сценарий деградации.

---

## 2. Инструменты (`tools_booking`, `tools_registry`)

- [ ] **2.1. Расширенные сценарии `get_available_slots`**
  - Текущая реализация поддерживает базовую фильтрацию по врачу/услуге и проверку `clinic_id`.
  - Хвосты:
    - поддержка диапазонов дат с разумными ограничениями (чтобы агент не запрашивал огромные интервалы);
    - продуманный формат ответа для long‑listing (например, ограничение на количество слотов, сортировка, агрегация).

- [ ] **2.2. Расширенные сценарии `create_booking`**
  - Сейчас используется `BookingCreateAdmin` и мапится часть полей.
  - Хвосты:
    - обработка предоплаты/оплаты (если по бизнесу допускается автоматическое создание записей с предоплатой/без неё);
    - явное логирование источника `"ai_agent"` в домене (например, отдельное поле/мета в `Booking` или связанный `AiToolEvent`).

- [ ] **2.3. Расширяемость реестра инструментов**
  - `get_default_tools_for_clinic` пока возвращает статический набор.
  - Требуется зафиксировать контракт для дальнейшего расширения:
    - возможность per‑clinic конфигурации доступных tools (через настройки клиники);
    - возможные будущие инструменты (`cancel_booking`, `reschedule_booking`, `create_task_from_conversation` и т.п.) — подготовить место/комментарии.

---

## 3. Логирование и аудит действий агента

- [ ] **3.1. Систематическое создание `AiToolEvent`**
  - В `run_ai_agent` и/или инструментах явно создавать `AiToolEvent`:
    - для каждого успешного и неуспешного вызова tools;
    - наполнять `tool_name`, `status`, `error_code`, `args` (обезличенные), `result`.
  - Обеспечить, чтобы это логирование было недорогим (batch‑insert или reuse session) и покрытым базовыми тестами.

- [ ] **3.2. Сервисные сообщения в чате**
  - Добавить сервисные сообщения вида:
    - `AI_ACTION_SUCCESS: Created booking #id`;
    - `AI_ACTION_ERROR: slot_conflict ...`,  
    чтобы оператору было прозрачно, что именно сделал агент.

---

## 4. Политика ПД и безопасность

- [ ] **4.1. Единое использование `AiSanitizer`**
  - Сейчас `AiSanitizer` уже подключён в оркестраторе; нужно убедиться, что:
    - вся история чата и дополнительные данные действительно проходят через санитайзер до отправки в LLM;
    - security‑тесты учитывают не только диалог, но и возможные вставки ФИО/контактов из доменных сущностей.

- [ ] **4.2. Ограничение на чувствительные операции**
  - Зафиксировать (и при необходимости реализовать) ограничения:
    - какие инструменты могут вызываться в каких режимах (`AUTO_REPLY` vs `SUGGEST_ONLY`);
    - требование ручного подтверждения некоторых операций (например, сложные reschedule/cancel) — через UI и/или задачи.

---

## 5. Тестирование и наблюдаемость

- [ ] **5.1. Дополнить интеграционные тесты оркестратора**
  - Расширить `test_ai_orchestrator_agent` сценариями:
    - multiple tool‑calls в одном цикле (например, сначала `get_available_slots`, затем `create_booking`);
    - выход по лимиту итераций и по таймауту;
    - корректное формирование `AgentResult` при ошибках инструментов.

- [ ] **5.2. Метрики и алёрты**
  - Проверить, что для AI‑агента снимаются метрики:
    - количество успешных/ошибочных сессий агента;
    - доля сценариев, закончившихся эскалацией на оператора.
  - При необходимости добавить прометей‑метрики и базовые алёрты.

