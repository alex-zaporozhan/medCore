## ARCH_DEV_LOY_FAMILY_013_TASKS — сверх‑детализированные dev‑таски

> Связанная архитектура: `ARCH_DEV_LOY_FAMILY_013.md`  
> Связанный DEV_PROMPT: `DEV_PROMPT_LOY_FAMILY_013` (P2, LOY‑1)

---

### 1. Understand — текущая реализация семейного/группового шэринга

1.1. **Инвентаризация Loyalty‑моделей, касающихся пакетов/депозитов/кошельков.**  
1.1.1. В доменном/ORM‑слое Loyalty (`src/domain/entities/*`, `src/infrastructure/db/*`) найти сущности:  
       - продукты/пакеты (`LoyaltyProduct`/`Package`), кошельки/депозиты (`LoyaltyWallet`/`Deposit`);  
       - операции (`LoyaltyOperation`, `LoyaltyTransaction`/`WriteOff` и аналоги).  
1.1.2. Для каждой выписать:  
       - поля владельца (`owner_patient_id`/`patient_id`), `clinic_id`;  
       - наличие/отсутствие полей для бенефициара/семейных связей.

1.2. **Поиск существующих механизмов «семейных»/групповых связей.**  
1.2.1. В коде (доменные модели, сервисы Loyalty/CRM, миграции) найти любые структуры/поля:  
       - `family`, `family_link`, `group`, `shared`, `relative`, `guardian` и т.п.;  
       - связи между пациентами, которые могут использоваться для шэринга.  
1.2.2. Зафиксировать, являются ли они реальными доменными моделями, временными флагами или сложившимися практиками в UI.

1.3. **Инвентаризация мест списаний по визитам с участием Loyalty.**  
1.3.1. В сервисах Loyalty списаний по визитам (и в Booking/ERP‑узле, если уже есть интеграция) найти:  
       - использование средств пакета/депозита для оплаты визита;  
       - случаи, когда пациент визита ≠ владелец пакета/депозита.  
1.3.2. Проверить, как сейчас хранится информация о том, кто фактически получил услугу (бенефициар) и есть ли привязка к семейным отношениям.

1.4. **Сопоставление с GAPS и ARCH‑артефактами.**  
1.4.1. Открыть `BACKEND_GAPS_Loyalty_NEXT.md` (LOY‑1) и UX‑заметки (`UX_FLOWS_AND_GAPS_NEXT.md`):  
       - выписать, какие кейсы семейного шэринга ломаются/вызывают вопросы.  
1.4.2. Сопоставить с `ARCH_LOYALTY_NEXT.md`, `ARCH_BOOKING_NEXT.md`, `ARCH_ERP_NEXT.md` и `ARCH_DEV_ERP_LOYALTY_011.md`, чтобы зафиксировать, какие ожидания уже описаны для FamilyLink/обязательств и отчётности.

---

### 2. Design‑to‑code — модели `FamilyLink` и расширения Loyalty

2.1. **Спроектировать доменную модель `FamilyLink`.**  
2.1.1. На уровне домена (Loyalty/CRM) оформить сущность `FamilyLink` с полями:  
       - `id`, `clinic_id`;  
       - `primary_patient_id` (владелец/основной пациент);  
       - `related_patient_id` (связанный пациент);  
       - `relation_type` (parent/child/spouse/guardian/other);  
       - права: `can_spend_from_owner_loyalty`, `can_view_owner_history` (bool/enum);  
       - опциональные лимиты: `spending_limit_total`, `spending_limit_periodic`, `valid_until`;  
       - служебные: `created_at`, `updated_at`, `created_by`, `is_active`.  
2.1.2. При необходимости спроектировать базовую модель групп (`LoyaltyGroup`) как надстройку над FamilyLink, но не усложнять v1 без явной потребности.

2.2. **Расширить модели ресурсов лояльности владельцем и бенефициаром.**  
2.2.1. В сущностях `LoyaltyWallet`/`LoyaltyPackage`/аналогах убедиться, что есть:  
       - `owner_patient_id`;  
       - `clinic_id`;  
       - (опционально) `group_id`.  
2.2.2. В сущностях операций (`LoyaltyOperation`/`WriteOff`/`Transaction`) добавить/уточнить поля:  
       - `wallet_id`/`package_id`;  
       - `beneficiary_patient_id` (кто получил услугу/benefit);  
       - `family_link_id` (если доступ был предоставлен через FamilyLink);  
       - `booking_id` (если связано с визитом).

2.3. **Определить правила доступа и проверки в операциях.**  
2.3.1. На уровне проектирования описать, как при попытке списания по визиту:  
       - если `patient_id` визита = `owner_patient_id` — действуют обычные правила;  
       - если они различаются:  
         - ищется актуальный `FamilyLink`/групповая связь между владельцем и пациентом визита;  
         - проверяются права/лимиты.  
2.3.2. Аналогично для просмотра истории: кто и при каких правах может видеть историю семейных расходов (родитель, супруг, клиника).

2.4. **Продумать стратегию миграции к новой модели.**  
2.4.1. Определить, как существующие шэринговые сценарии (если есть) будут отражены через `FamilyLink`:  
       - какие текущие поля/структуры будут маппиться на `FamilyLink`;  
       - какие данные не могут быть восстановлены и останутся только в истории.  
2.4.2. Зафиксировать миграционную стратегию (в коде/плане) так, чтобы не потерять юридически важную информацию по прошлым операциям.

---

### 3. Implement — сервисы FamilyLink и обновление Loyalty‑логики

3.1. **Реализовать сервис управления FamilyLink.**  
3.1.1. Создать `FamilyLinkService` (или расширить существующий Loyalty/CRM‑сервис) с методами:  
       - `create_family_link(...)`;  
       - `update_family_link(...)`;  
       - `deactivate_family_link(...)`;  
       - методы выборки связей для пациента/семьи.  
3.1.2. Обеспечить, чтобы все операции проверяли:  
       - `clinic_id` принадлежности пациентов;  
       - пермишены на управление семейными связями (см. SEC/RBAC).

3.2. **Внедрить проверки FamilyLink в списаниях по визитам.**  
3.2.1. В сервисах списаний Loyalty (в т.ч. интеграции с ERP‑узлом из `DEV_PROMPT_ERP_LOYALTY_011`):  
       - перед списанием по пакету/депозиту:  
         - определить владельца ресурса;  
         - если владелец ≠ `patient_id` визита — проверить FamilyLink (и лимиты) перед списанием;  
       - при успешном списании сохранять `beneficiary_patient_id` и `family_link_id`.  
3.2.2. При нарушении прав/лимитов возвращать контролируемую ошибку/варнинг, не выполняя списание.

3.3. **Обновить получение истории по семейным ресурсам.**  
3.3.1. В API/сервисах, отдающих историю операций пакетов/кошельков:  
       - учитывать права просмотра по FamilyLink (родитель/опекун/супруг);  
       - фильтровать данные по `clinic_id` и RBAC.  
3.3.2. Обеспечить, чтобы интерфейсы истории могли отображать операции с указанием, за какого бенефициара они были совершены (без нарушения ПД‑политики).

3.4. **Минимально обновить Booking/ERP‑связку под новую модель.**  
3.4.1. В точках взаимодействия Booking/ERP с Loyalty (завершение визита, ERP‑узел):  
       - убедиться, что передаётся информация о владельце/бенефициаре ресурса;  
       - согласовать формат с `ARCH_DEV_ERP_LOYALTY_011.md`, чтобы ERP‑обязательства отражали семейный контур корретно.  
3.4.2. Добавить, где нужно, поля/связи (`owner_patient_id`/`beneficiary_patient_id`) в ERP‑слое для дальнейшей отчётности.

---

### 4. Integrate — UI/Omni/Tasks вокруг семейного шэринга

4.1. **Интеграция FamilyLink в админский UI (Loyalty/CRM).**  
4.1.1. Спроектировать и реализовать (отдельно в фронтовых задачах) базовый UI:  
       - просмотр/редактирование семейных связей пациента;  
       - настройка прав (тратить/смотреть историю, лимиты);  
       - отображение, какие пациенты привязаны к какому пакету/кошельку.  
4.1.2. Убедиться, что API, реализованные в 3.1/3.3, покрывают эти сценарии без обхода сервисов.

4.2. **Интеграция с Omnichannel/CRM‑сценариями.**  
4.2.1. В Omnichannel/CRM‑контексте (по `ARCH_OMNICHANNEL_NEXT.md`, `ARCH_CRM_NEXT.md`):  
       - обеспечить возможность просматривать статус семейных пакетов/кошельков при общении с любым из бенефициаров;  
       - использовать FamilyLink для подсказок (например, кто может записать ребёнка, кто может оплачивать).  
4.2.2. Подготовить точки расширения для `DEV_PROMPT_LOY_AI_014` (чтобы AI‑кампании могли учитывать структуру FamilyLink).

4.3. **Связь с Tasks/Attention и OBS‑цепочками.**  
4.3.1. С учётом раздела 5 ARCH‑дока обозначить:  
       - события, по которым создаются Attention/Tasks (аномальные списания, всплески трат, истекающие семейные балансы);  
       - минимальный набор метрик по семейному шэрингу (количество активных FamilyLink, операции по ним).  
4.3.2. Убедиться, что Tasks/Attention согласованы с `ARCH_DEV_TASKS_MODEL_020.md` и готовы к использованию в `DEV_PROMPT_TASKS_AI_021`.

---

### 5. Observe — логи, метрики и безопасность FamilyLink

5.1. **Добавить структурированное логирование по операциям FamilyLink.**  
5.1.1. В `FamilyLinkService` и сервисах списаний логировать:  
       - создание/изменение/деактивацию `FamilyLink` (clinic, owner, related, тип связи, права);  
       - ключевые операции списаний, где использован FamilyLink (ссылка на владельца/бенефициара/ресурс, без ПД).  
5.1.2. Убедиться, что логи не содержат ФИО/контактов, только идентификаторы/агрегированные данные.

5.2. **Метрики по использованию семейного шэринга.**  
5.2.1. Ввести базовые метрики:  
       - количество активных `FamilyLink` по клиникам;  
       - объём списаний по семейным ресурсам (разделение по владельцам/бенефициарам);  
       - частота отказов по причинам FamilyLink (нет прав, превышены лимиты).  
5.2.2. Связать эти метрики с OBS‑цепочками `ARCH_DEV_OBS_CHAINS_023.md` для оценки влияния семейного шэринга на retention/выручку.

5.3. **SEC/RBAC и Tasks/Attention по аномалиям.**  
5.3.1. Убедиться, что операции управления FamilyLink и доступа к истории семейных ресурсов:  
       - защищены пермишенами согласно `ARCH_DEV_SEC_RBAC_022.md`;  
       - проверяют `clinic_id` принадлежность.  
5.3.2. Для аномальных ситуаций (частые отказанные списания, подозрительные всплески) реализовать генерацию Attention/Tasks с типами вроде `LOYALTY_FAMILY_RISK`, с привязкой к клинике и пациентам.

---

### 6. Stabilize — тесты, миграции и обновление артефактов

6.1. **Миграции и обратная совместимость данных.**  
6.1.1. Подготовить миграции для `FamilyLink` и расширений сущностей Loyalty:  
       - добавление новых таблиц/полей с безопасными дефолтами;  
       - при необходимости — инициализация базовых семейных связей из текущих данных (если есть явно выраженные паттерны).  
6.1.2. Документировать ограничения миграции (где старые сценарии шэринга остаются только в истории и как они будут обрабатываться).

6.2. **Покрыть FamilyLink и списания тестами.**  
6.2.1. Написать/обновить unit‑тесты:  
       - для `FamilyLinkService` (создание/изменение/деактивация, права, лимиты);  
       - для сервисов списаний с FamilyLink (позитивные и негативные кейсы).  
6.2.2. Добавить интеграционные тесты связки Booking → Loyalty → ERP с участием семейных пакетов/депозитов:  
       - родитель оплачивает визиты ребёнка;  
       - супруги делят общий депозит.

6.3. **Синхронизация документации и GAPS.**  
6.3.1. После завершения реализации обновить:  
       - `DEV_PROMPTS_NEXT.md` — статус `DEV_PROMPT_LOY_FAMILY_013`;  
       - `BACKEND_GAPS_Loyalty_NEXT.md` — отметить закрытые/уточнённые пункты LOY‑1;  
       - `UX_FLOWS_AND_GAPS_NEXT.md` — отразить новый UX‑уровень семейного шэринга;  
       - при необходимости `ARCH_AUDIT_NEXT.md` — описание модели FamilyLink и её влияния на ERP/Booking/Tasks.  
6.3.2. Убедиться, что реализованная модель прямолинейно поддерживает последующие DEV_PROMPT (`ERP_LOYALTY_011`, `LOY_AI_014`, `TASKS_MODEL_020`, `TASKS_AI_021`, `PERF_SPOTS_024`).

---

## Выполнено (v1 backend, @QA_ARCH / 2026‑03)

Ниже — факт кода; детальный список файлов/миграций не дублируется (см. репозиторий).

| Область | Сделано |
|--------|---------|
| **§2.1** | Таблица **`family_links`**, ORM **`FamilyLink`**, миграция **`d3e4f5a6b7c8`**; поля **`beneficiary_patient_id`**, **`family_link_id`** на `subscription_usages`, `wallet_transactions`, `erp_loyalty_obligation_movements`. |
| **§2.2 / 2.3** | Владелец ресурса = `CustomerSubscription.patient_id` / `Wallet.patient_id`; операции дополнены бенефициаром и ссылкой на связь. **`LoyaltyWallet`** в коде = сущность **`Wallet`** (`owner` = `patient_id`). |
| **§3.1** | **`FamilyLinkService`**: CRUD/деактивация, выборка, лимиты (в т.ч. **комбинированные денежные**: подписка + кошелёк по одному `family_link_id`). |
| **§3.2** | **`LoyaltyService`**: подписка + **`PackageFamilyLink`** или **`FamilyLink`**; **`FamilySpendDenied`**; booking передаёт **`beneficiary_patient_id`**. |
| **§3.2 кошелёк** | **`WalletService.spend_points`**: опциональный бенефициар, проверка FamilyLink, заполнение колонок транзакции; **`WalletFamilySpendDenied`**. |
| **§3.3** | **`GET /patient/loyalty/history`**: `can_view_owner_history`, детали **`timeline_view`**, бенефициар; admin **`/subscription-usages`** — владелец или бенефициар. |
| **§3.4** | **`RegisterWriteOffForVisitInput`** и движения ERP с бенефициаром/`family_link_id` (обязательство по-прежнему на владельца продажи). |
| **§5.1–5.2** | Логи без ПД; метрики: откачи семейного списания, **path** (`owner` / `package_member` / `family_link`), lifecycle создания/деактивации связи. |
| **§6.1** | Миграция с nullable‑полями; **`package_family_links`** сохранены (два механизма параллельно). |
| **§6.2** | Unit‑тесты **`tests/services/test_family_link_loyalty.py`** (подписка, кошелёк, таймлайн). |

**Не сделано по этому чеклисту (см. «На потом»):** §1 (письменная инвентаризация в GAPS/UX), §2.1.2 `LoyaltyGroup`, §2.2.1 опциональный `group_id`, §2.4 документирование миграции данных, §4 UI/Omni/Tasks, §5.3.2 Attention, §6.2.2 интеграционный e2e, §6.3 обновление **`DEV_PROMPTS_NEXT`**, **`UX_FLOWS_AND_GAPS_NEXT`**.

---

## На потом (@QA_ARCH — предложения и новые риски)

1. **Документация и GAPS:** закрыть §6.3 — `DEV_PROMPTS_NEXT.md`, `BACKEND_GAPS_Loyalty_NEXT.md` (LOY‑1), `UX_FLOWS_AND_GAPS_NEXT.md`; при необходимости `ARCH_AUDIT_NEXT.md`.
2. **Два механизма шэринга:** явно описать политику сосуществования **`PackageFamilyLink`** и **`FamilyLink`** (когда что заводить; опциональная выгрузка/миграция в единую модель).
3. **RBAC:** отдельный permission (например `manage_family_links`) + миграция seed, вместо только `manage_loyalty`.
4. **ERP obligation:** при необходимости отчётности — поля бенефициара на уровне **`ErpLoyaltyObligation`** (не только на movement).
5. **OBS:** gauge «активных FamilyLink по клинике» (сейчас lifecycle counters; без периодического job — не snapshot).
6. **Attention/Tasks:** тип вроде **`LOYALTY_FAMILY_RISK`** при порогах отказов/аномалий; связка с `ARCH_DEV_TASKS_MODEL_020`.
7. **Интеграционные тесты:** полная цепочка Booking → Loyalty → ERP с семейным сценарием; сценарий «супруги / общий депозит» на кошельке — прогон в CI.
8. **Admin UI / PWA:** экраны FamilyLink; отображение `timeline_view` и бенефициара в клиентских компонентах лояльности.
9. **Omnichannel / CRM / LOY_AI_014:** подсказки по семье, контекст пакетов при диалоге с бенефициаром.
10. **Лимиты:** разнести семантику **сумма vs визиты** (сейчас один numeric для обоих в краевых случаях); явно задокументировать **UTC‑месяц** для `spending_limit_periodic`.
11. **ПД:** политика отображения «за кого списание» в истории для родственника (только id vs маскированные подписи).
12. **Валидация API:** жёсткая проверка `beneficiary_patient_id` == пациент визита на публичных входах, если появятся вне booking.
13. **Обратная связь с PERF/OBS:** дашборды по новым метрикам семейного шэринга.

---

